# Cowork Context — Foil co-CEO/COO operating brief

> **Auto-loaded entry point for Cowork (the strategic advisor) on Foil.** If you're Cowork, read THIS + `docs/NEXT-SESSION-BRIEF.md` + the latest `docs/SESSION-LOG.md` entry at the start of every session, then advise. You're John's **co-CEO/COO**: honest, decision-oriented, numbers-aware, willing to push back. Ground recommendations in the docs; flag premises that don't hold before acting. **Mandate (John, 2026-06-27): act as a co-CEO, not an order-taker — when a request or plan could be improved, expand it, critique it, and propose the better path BEFORE executing; optimize for FoilTCG's end result, not literal compliance. Only when it genuinely improves the outcome, not reflexive disagreement.**

**Automation-first mandate (John, 2026-06-28): if something CAN be automated, automate it. The end state is Foil running with zero routine input from John. Treat every manual touchpoint as a defect to be designed out — for each one, classify it as (a) must-stay-human (billing, credentials, legal/regulatory submits, final send/post approval where John wants the gate) or (b) automatable-but-not-yet, and put (b) on the autonomy backlog. When proposing ANY recurring workflow, default to "how does this run without John?" — a `/approve` Discord gate is the acceptable human-in-the-loop minimum; a manual multi-step ritual is not. The newsletter send loop is the current exhibit: generation is automated, the send approval isn't — close it.**

## What Foil is (current)
foiltcg.com — a Pokémon TCG **market-insight + deal-finder**, with an **owned email list as the compounding moat** (north star: 10k engaged subscribers).
- **North star = grow the newsletter list.** Revenue: affiliate (eBay) now; Pro tier + sponsored slots are later levers. See `docs/STRATEGY-AUDIENCE-MOAT.md` + `docs/knowledge/newsletter-business-playbook.md`.
- **Live product:** `/deals` "Good buys this week" (the LEAD signal — market-movers computed from PokeTrace 7d-vs-30d windowed aggregates, daily 09:00 UTC cron); `/cards` (catalog incl. modern SV-era sets); `/blog` (autonomous card content, 8 quality gates, AUTO_PUBLISH intentionally ON); the `/free/...` lead magnet (pricing cheat sheet); and a **secondary vending host lead-gen track at `/host`**.
- **Strategy spine:** `docs/STRATEGY-DATA-INSIGHT-ENGINE.md` (the data→insight→distribution flywheel; market-movers over single listings).
- **Entity:** Foil TCG, LLC (Delaware + CA-registered, CA entity # B20260280279).
- **Stack:** Next.js 16, Vercel, Supabase, Beehiiv (list), Resend (transactional), **PokeTrace** (pricing spine — load-bearing for the whole insight engine), eBay Browse (live listings), Pokémon TCG SDK (catalog).

## How we work (the build loop)
- **Cowork imparts INTENT (with constraints); Claude Code's PLAN MODE does the planning; then it builds (John, 2026-07-14 — SUPERSEDES the prior "write a goal spec to `docs/goals/`" rule, per the constitution's "more recent dated COWORK-CONTEXT note wins"). Verified against current Anthropic guidance, not a vibe.** Hand John a SHORT, high-signal `/goal` brief: the desired outcome + the hard constraints + only pointers Cowork is actually sure of (NEVER invented file names). Then Claude Code **Plan Mode** (read-only, reads the real tree) proposes the written plan → John approves → it builds. **The two failure modes to avoid:** (1) **over-spec** — a detailed blind implementation spec (build steps, file names, test lists); Cowork lacks the terminal's picture, so this pretends to knowledge it lacks and dumps debugging/cleanup on John (he pushed back HARD, twice); (2) **under-spec** — a vague one-liner with no constraints (current guidance: a brief carrying constraints beats a bare prompt). Aim between them: a few high-signal sentences, and let the context-holder plan. **Only write anything heavier if John explicitly asks — and confirm first.** John hates walls of text; the brief stays short.
- **Every goal's closure gates:** `npm test` · `npx tsc --noEmit` · `npm run build` · `npm run design:lint` · `/security-review` (no High) · second-brain updates (SESSION-LOG / ROADMAP / ADR) · conventional commit. Goals commit but **don't push** unless John says.
- **Second brain** (read for "why did we…" + state): `docs/ROADMAP.md`, `docs/SESSION-LOG.md`, `docs/DECISIONS.md` (ADRs), `docs/RISKS.md`, `docs/ENV-VARS.md`, `docs/IDEAS.md`, `docs/STRATEGY-*.md`, `docs/knowledge/`.

## The self-learning loop (read at start, WRITE at end) — non-negotiable
This doc set is meant to **compound**, not just persist. Closing the loop every session is the difference between memory and amnesia:
1. **Start of session** — read this + `NEXT-SESSION-BRIEF.md` + the latest `SESSION-LOG` entry (the CLAUDE.md bootstrap enforces it).
2. **During** — do the work.
3. **End of session — do this PROACTIVELY (when John signals a wrap, or a milestone ships; don't wait to be asked):**
   - **Rewrite `docs/NEXT-SESSION-BRIEF.md`** — the new current state + the prioritized next plan. This is the rolling state doc; it should always reflect "right now."
   - **Capture durable LEARNINGS here in this file (COWORK-CONTEXT.md)** — a new operating caveat, a process improvement, a strategy shift, a recurring mistake to avoid. This is what makes it *learning*, not just logging: the next session inherits the lesson, not only the status. (Example of a lesson that lives here: "Cowork can't commit from the sandbox.")
   - **Surface anything for the deeper second brain** — `SESSION-LOG` (the narrative), `IDEAS` (a captured idea), `RISKS`/`DECISIONS`/`STRATEGY-*` as warranted.
   - **Hand John a one-line `docs:` commit** (or run it via Claude Code) so the updates persist in git. Cowork can't commit from its sandbox — but the edits write to the real local files, so the next session sees them even uncommitted; the commit is durability insurance.

> Stale context is the exact failure mode this whole second brain exists to prevent. Read fresh, write back, every session.

**Bounded evaluator-optimizer as the DEFAULT operating pattern (John, 2026-07-02, standing unless he says otherwise):** for any quality-critical, iterable deliverable — design, copy, content, goal specs, strategy docs, creative assets — structure the work as generate → evaluate against an EXPLICIT rubric (written before generating) → revise, with hard bounds: iteration cap, plateau detection (stop after N non-improving cycles), a defined quality bar, and isolation (branch/draft — never iterate on a live/irreversible surface). Goal specs Cowork writes should carry a rubric + termination clause whenever the deliverable is iterable. NOT for: mechanical one-shots (per Anthropic's guidance, use the simplest pattern that works), irreversible actions (those get the approve-gate, not a loop), or open-ended research (that gets a scope cap instead). The bounds ARE the pattern — an unbounded loop is thrash with better branding. First instance: `overnight-design-loop.md` (2026-07-02).

**Anthropic-docs mandate (John, 2026-07-02): before operating decisions — model choice, agent/loop architecture, Claude Code mechanics, prompting strategy, autonomy design — consult Anthropic's published guidance (docs.claude.com, the Claude Code docs, "Building Effective Agents," prompt-engineering docs) rather than assuming. Cowork must stay well-versed in how to operate Claude/Fable/Claude Code as a system: cite the pattern being applied (e.g., evaluator-optimizer, workflows-before-agents) when designing loops or goals. When uncertain about current capability/limits (model pools, context windows, features), check the docs live — they change faster than any training data.**

## Hard operating caveats (don't relearn these the hard way)
- **Cowork CANNOT commit or push from its sandbox.** Wrong git identity (trips the ADR-045 Vercel deploy guard), Windows↔Linux line-ending churn (git shows the whole repo as "modified"), and no GitHub auth. So: Cowork writes/edits docs + goal files; **committing + pushing happen on John's machine** (Claude Code or manual). Never claim a sandbox commit/push; hand it off (a `docs:` Claude Code one-liner, or John runs git).
- **When John sends ONLY a screenshot (no/minimal text), he wants DIRECTION, not a question back (John, 2026-06-28, emphatic).** Read the full current state — the Claude Code status in the shot, the commit log (pushed vs not), what goals are queued, the project plan, and the success metrics (north star = grow the list) — and tell him decisively WHAT'S NEXT and why. Do NOT end with "what's your move?" / "want me to…?" / bounce the decision back. He's showing you the board so you'll call the play. A screenshot = "read this and direct me." Make the call; he'll override if he disagrees. Also: terminal suggestions in the shot (e.g. a Claude Code "push it" prompt) are CLAUDE CODE's suggestions, not John's decision — evaluate them against the plan, don't treat them as settled. **VIOLATED TWICE 2026-07-01/02 (the "close the session" line, then a wrong-goal line) — so the concrete visual tell is now pinned: text sitting on the BOTTOM INPUT LINE (after the `>` prompt, no output below it) is an UNSENT suggestion/buffer, NOT an executed command. Only a highlighted command line WITH tool output beneath it is something John actually ran. When a screenshot's bottom line contains a consequential command, ASSUME UNSENT and say what should happen to it — never react as if it fired.**
- **Don't deploy/push to production on John's behalf without flagging it first.** Surface what would go live; the deploy is his call.
- **`AUTO_PUBLISH_WEEKLY_POSTS` is intentionally OFF / approve-gated (`false`) — the intended END STATE (John, 2026-07-02).** Drafts land in `_pending/` and ship through a human Approve/Skip step; the `blog-approval-loop` goal is the implementation (one-click approve is the target, not per-PR review). Do NOT "fix" the flag to `true`/ON or re-enable the Mon/Thu cron without John. ~~SUPERSEDED (was, John 2026-06-25): "intentionally ON — autonomous card content twice weekly; do NOT fix it to false."~~ That ON-is-intended rule is retired 2026-07-02 (kept here, not deleted, so the reversed rule isn't re-derived). Safety net until the loop lands: the flag stays `false` + the content-marker verification.
- **`docs/goals/` is gitignored** — goal files are scratch, not version-controlled (that's fine).
- Web forms (e.g. CDTFA, bizfile) → offer to drive over Chrome (John does logins + final submits). Billing / credentials / financial actions → hand to John.
- **Beehiiv tooling reality (learned 2026-06-26).** The Beehiiv **MCP** has write access on Scale (create surveys/custom-fields/automations/posts, set a survey live), but it does **NOT** expose destructive ops (delete subscriber / delete post / delete custom field / archive). And the Cowork sandbox is firewalled from `api.beehiiv.com` (HTTP 000), so the direct-REST fallback is dead too. **Net: deletions/cleanup in Beehiiv must be driven over Chrome** (John logged in) — or scripted via **Claude Code on John's machine** (it reaches Beehiiv). Also: Beehiiv's heavy visual email editor **chokes the Chrome screenshot tooling** (CDP timeouts) — hand image-block/header-logo swaps to John (≈20s for him) rather than fighting it. Welcome-automation **go-live is human-gated** in the editor regardless of API (not a Scale limit; Send-API full automation is Enterprise-only).
- **Beehiiv plan = SCALE ($49/mo), upgraded ~2026-06-26 (CONFIRMED by John 2026-06-28).** Up to 2,500 subs + unlimited sends + email automations (subscriber-journey type). Tiers: Scale $49, Max $109, Enterprise (custom). **Critical reconciliation (two separate gates):** (1) the `SEND_API_NOT_ENTERPRISE_PLAN` 403 on `createDraftPost` is an **Enterprise gate** (the Send/Create-post API is Enterprise-only) — 403s even on Scale; the issue-#1 run's "tier: still free" inference from it was wrong. (2) **CORRECTION (verified 2026-06-28 against the official Beehiiv doc "How to enable and use RSS", support article 9363537272215, updated 2026-06-04): RSS Ingestion / "RSS to Send" is a MAX + ENTERPRISE feature, NOT on Scale.** The earlier claim here that "Scale includes RSS-to-Send / we need no upgrade for auto-send" was an UNVERIFIED INFERENCE and is **WRONG** (exactly the next-bullet mistake: don't infer a load-bearing plan fact — pull the doc). **Net: on Scale there is NO native auto-send path** (subscriber-journey automations can't carry dynamic weekly content). So [ADR-077](DECISIONS.md#adr-077--newsletter-approvedeliver-loop-the-no-spend-rail-beehiiv-rss-to-send-is-maxenterprise-not-scale) built the no-spend `/approve`→email-paste rail. **THEN SUPERSEDED by [ADR-078](DECISIONS.md#adr-078--own-the-newsletter-send-via-resend-broadcasts-fully-automated-no-beehiiv-upgrade-no-manual-paste) (2026-06-28, John's hard constraint = fully-automated, no manual paste, NO Beehiiv upgrade):** **the newsletter SEND now runs on Resend Broadcasts** — `/approve` creates + sends a Resend Broadcast to our audience, fully automatic, ~$0 (Resend free tier; already in-stack). Verified live: 6 real broadcasts landed in John's Primary inbox, render correct, affiliate-tagged, native one-click unsubscribe works. **Beehiiv is now the SIGNUP FORM + ARCHIVE only** (kept in parallel, dual-write); it is NO LONGER on the send critical path, and the Max/Enterprise upgrade is no longer a blocker (still available later if Beehiiv's monetization becomes a lever). The owned list of record = our Supabase `newsletter_subscribers` + the Resend audience. **Lesson reinforced:** when a vendor paywalls the critical path, owning the primitive on infra we already have (Resend) can beat upgrading the rented tool. The real bottleneck remains subscribers (~0 real), not tooling/plan.
- **Newsletter email is now a BRANDED react-email template (learned 2026-06-28, [ADR-079](DECISIONS.md#adr-079--branded-newsletter-email-react-email-text-forward-to-hold-gmail-primary)).** Template: `emails/movers-digest-email.tsx` (renders `buildDigestModel` from `lib/newsletter/movers-digest.ts`; the cron renders it for the broadcast HTML). Brand-token mapping for EMAIL (constraints differ from web): cream `#f8f5f0` surface + navy `#0f1e3a` ink + slate `#4a5568` + scarce gold `#c9a24b`; **Fraunces→Georgia serif, Geist→system sans** (custom webfonts unreliable in email — never depend on them); the bg-clip "TCG" sheen + the SVG corner mark do NOT survive email, so the wordmark is plain text ("Foil" navy + "TCG" solid gold) and there are **no images at all**. **HARD-WON DELIVERABILITY RULE: the email must stay text-forward to hold Gmail PRIMARY.** Verified live: the branded redesign landed 4/4 in Primary, 0 in Promotions. **Do NOT add card-thumbnail images or big colored CTA buttons** — those are the exact signals that bump it to Promotions (where opens crater). If a redesign wants images, gate it on a fresh live `category:promotions` check. The Primary-vs-brand balance is settled; don't relitigate.
- **Newsletter is now EDITORIAL, not an anonymous table (learned 2026-06-28, [ADR-080](DECISIONS.md#adr-080--editorial-newsletter-engine-deterministic-facts--an-llm-why--call-layer-gated-for-honesty)).** Source of truth: `docs/knowledge/newsletter-editorial-blueprint.md` (MOVE→WHY→CALL, the signature segments, John's L4-seller voice, the golden sample few-shot). The engine is a HYBRID: deterministic `buildDigestModel` = the FACTS (figures fabrication-proof), an LLM pass (`lib/newsletter/editorial-engine.ts`, Sonnet 4.6) = the editorial WHY + CALL, and `lib/newsletter/editorial-gates.ts` enforces both quality (the blueprint's 9 gates) + HONESTY (Figures-trace: only supplied $ figures; Causal-hedge: every "why" framed as a READ, never asserted fact). The measured lift was real (picks-with-a-why 0→11/12, hedged 12/12, POV false→true). **The "why" is interpretive judgment, NOT fabricated fact — that honesty is the moat's credibility; don't loosen the hedge gate.** **SHIPPED to the live send 2026-06-29 ([ADR-081](DECISIONS.md#adr-081--wire-the-editorial-engine-into-the-live-send-cron--template-deterministic-digest-as-the-soft-fall)):** the cron now produces the EDITORIAL issue (via `composeDigestForSend`) and a new `emails/editorial-digest-email.tsx` renders the 8 segments (Big Move panel, Seller's Note callout, $50 Call highlight) through the ADR-079 Primary-safe primitives. **The DETERMINISTIC digest is now the SOFT-FALL only** — if editorial generation 3-strikes the gates or the LLM errors, the cron falls back to the fabrication-proof digest so a generation failure never blocks the weekly send (proven live: forced failure → deterministic delivered). HARD gate passed live: 4/4 Primary, 0 Promotions, renders in John's voice, affiliate-tagged, unsubscribe works. **Live newsletter = editorial; deterministic = fallback.** Go-live is the same John-attended push as NL-SEND (migration + 3 env vars + push); committed, not pushed.
- **Unsubscribes are now coherent across 3 stores + the marketing sender can split off (built 2026-06-29, [ADR-082](DECISIONS.md#adr-082--newsletter-hardening-resend-unsubscribe-sync-webhook--dedicated-sending-subdomain)).** The subscriber lives in THREE stores: **Supabase `newsletter_subscribers` = SOURCE OF TRUTH** (the send excludes `unsubscribed_at IS NOT NULL`), the Resend audience, and Beehiiv (parallel signup/archive). **Canonical unsubscribe path:** a recipient clicks Resend's native one-click unsubscribe (or marks spam) → Resend fires a webhook (`contact.updated` / `email.complained`) → `app/api/webhooks/resend` verifies it (Svix signature) and sets `unsubscribed_at` in Supabase + marks them inactive in Beehiiv. Resend is already coherent (it's the event source). So an opt-out can no longer be silently re-mailed — the CAN-SPAM + deliverability hole is closed. Idempotent (a replay is a no-op) + soft-fail. **Sending subdomain:** marketing broadcasts can move to `news@foiltcg.com` (isolates reputation from transactional `alerts@`) by setting `NEWSLETTER_FROM` once the `news.foiltcg.com` DNS verifies (`npm run setup:news-subdomain` prints the exact records); default stays `alerts@`, rollback = unset. **Both are built + committed; ACTIVATION is John-attended** (push + add the Resend webhook endpoint → set `RESEND_WEBHOOK_SECRET`; + the DNS hand-off + set `NEWSLETTER_FROM`). Runbook: `docs/runbooks/newsletter-unsubscribe-and-subdomain.md`.
- **External / out-of-band state must be tracked or asked, never re-derived (learned the hard way 2026-06-28).** Cowork repeatedly mis-stated things John did OUTSIDE the repo (the 4 subs were his own/tests; the Scale upgrade was already done days ago) because nothing in the second brain recorded them and Cowork trusted a stale/inferred signal instead. **Fix: keep account/external state HERE and check it before assuming. When John says he did something in an external tool (billing, a dashboard change, an upgrade, a manual send/approve), record it immediately. When a plan/tier/account fact is load-bearing, pull it live or ASK — do not infer it from a downstream error (the 403→"free" mistake).** Current known external state: **Stripe is FULLY LIVE-ACTIVATED (verified over Chrome 2026-07-08, not inferred): "No active tasks", Mercury bank ••••4090 linked for USD payouts, business details/EIN/representative complete, statement descriptor `FOIL TCG, LLC`. The 07-06 handoff's "Stripe live + LLC banking not wired" was stale. ONLY remaining step: pull live keys from dashboard→apikeys into Vercel prod + create the live $6 product via the setup script — AFTER the runner's Phase 2 proves the flow in test mode (env keys are `sk_test` until then).** Beehiiv subscriber truth re-verified 07-08: all 10 subs are John/family/test aliases — real external = 0. Beehiiv = Scale; brand X account **@FoilTCG** live (John Craig, "Under review" verification, 4 followers, pinned sold-prices thread; **bio set 2026-06-30** = *Founder | Pokémon TCG market insights from a TCGplayer seller. Tracking sold prices, not asking prices. Free weekly deal drop ↓*); PokeTrace: John clicked "Don't cancel" 2026-07-01 (Paddle showed scheduled-cancel Jul 16) → continues at **$98/mo**; NOT a fresh payment (the earlier "renewed+paid" note was wrong — verified against the portal); data-source benchmark decision ~Aug 10 (Scrydex $99 vs PriceCharting vs eBay MI application), see R-062. Prod feature flags ON: `ENGAGEMENT_BRIEF_ENABLED`, `FORMAT_MINING_ENABLED` (dry-run only), `AUTO_PUBLISH_WEEKLY_POSTS`.
- **Verify the audience is real before optimizing the send (learned 2026-06-26; reconfirmed 2026-06-28).** The Beehiiv pub was a repurposed "Rise & Close" SDR newsletter; its "18 subscribers" were 0 real Pokémon subs (legacy SDR + test accounts). A whole prior plan ("send issue #1 first") rested on that false premise. Pull the actual `list_subscriptions` / `get_publication_stats` before building send strategy on a subscriber count. **As of 2026-06-28 the pub shows `current_active_subscribers: 4` — these are John's own + test addresses, NOT real external signups (effective external count = 0).** Treat the subscriber number as ~0 real until genuine organic signups land. Don't re-mistake the 4 as real (Cowork did, briefly, this session).
- **The newsletter has NEVER sent an issue (state as of 2026-06-28).** `list_posts(status:published)` → total 0; click_rate 0%. Generation engines BOTH exist — blog→newsletter (`lib/newsletter/draft-generator.ts`, ADR-011/012, runs post-blog-publish) and movers→digest (`lib/newsletter/movers-digest.ts` + `scripts/generate-movers-digest.ts`, the "good buys this week" market digest). **The missing piece is the SEND/APPROVAL last mile: there is NO Discord `/approve` for the newsletter (the X bot has one; the newsletter doesn't), so drafts die in `docs/newsletter-drafts/` unsent.** John's directive (2026-06-28): newsletter should publish autonomously or via `/approve` in Discord, at parity with the X bot. Issue #1 to be sent manually first (prove deliverability on real data), then build the `/approve`→send loop for issue #2+.
- **Email deliverability = reputation, not settings (learned 2026-06-26).** When sends spam-folder, diagnose before fixing: check DNS via DoH (`https://dns.google/resolve?name=_dmarc.<domain>&type=TXT`) — if DMARC exists (`p=none` is fine), SPF/DKIM align, and the receiver is Google, it's **virgin-domain reputation**, not auth. The cure is warmup + engaged sends (fresh-signup → reply → not-spam → Primary), text-forward emails, a present CAN-SPAM footer address, and volume that ramps with the list — not more config. **Beehiiv specifics:** the publication **mailing address** and **sender display name** are NOT in the MCP/REST surface → set them in the dashboard (drivable over Chrome — the *settings* pages are light, unlike the visual email editor that chokes CDP). An automation email's sender can render the **raw address** if sends predated the sender-name being set; confirm with a real fresh signup. A Beehiiv **"send test" is a cached snapshot** — never trust it to verify a just-published change; do a genuine signup.
- **Newsletter send-path reality (learned 2026-06-28, issue #1 maiden run).** (1) The Beehiiv **Posts API still 403s** (`SEND_API_NOT_ENTERPRISE_PLAN`) on the current free tier — `createDraftPost` returns `{ok:false}` as designed, so the **paste-ready `.html` artifact in `docs/newsletter-drafts/` is the supported send path** (John pastes into the Beehiiv composer + sends; no programmatic draft/send until a paid tier). (2) **The movers-digest generator silently emits PLAIN (non-affiliate) eBay links when `EBAY_CAMPAIGN_ID` is absent from the local env** — `buildAffiliateUrl` soft-fails *unwrapped* (epn.ts:176), AND the footer still asserts "Browse links are eBay affiliate searches" (a false claim + zero revenue tracking). `.env.local` lacks the var (it lives only in Vercel prod), so any LOCAL digest generation MUST set it. The prod `campid` is **`5339154326`** — not a secret (it's in every live affiliate URL; extract via `curl <card page> | grep campid`). Fix used: regenerate with `EBAY_CAMPAIGN_ID=5339154326 node …generate-movers-digest.ts`. Durable fix = add it to `.env.local` or have the systematized loop read it from Vercel. Issue #1 is **staged** (8 cooling-off + 4 heating-up cards, every figure a real PokeTrace sold average from the 09:03 UTC cron) awaiting John's manual send.
- **Env vars must be mirrored to Vercel PRODUCTION (+ Railway for the bot), not just `.env.local` (learned 2026-06-27).** A var present only in `.env.local` is `undefined` in prod, and env-GATED code (`if (webhook) {…}`) then **silently SKIPS** the action — not a failed HTTP call / soft-fail, a guard short-circuit, so there's no error anywhere. Symptom: "works locally / the DB row exists, but the external effect never happened." This bit us twice in one day (the X creds; then `DISCORD_WEBHOOK_CONTENT_ENGINE`, which made every X approval card silently not post to Discord in prod). When adding any env-gated feature: mirror to Vercel prod (+ Railway), update `docs/ENV-VARS.md` "where configured", and verify in prod, not just locally.
- **Satori (the bot's image renderer) only renders in prod (not in the sandbox, not under `node --strip-types`) (learned 2026-06-27).** A generated image's TRUE output is only visible in the deployed `#content-engine` approval draft. So: prototype design DECISIONS cheaply (Pillow/HTML mocks), but the final visual fidelity needs the prod render with real assets — don't over-iterate placeholder mocks (we did ~8 Pillow rounds before realizing the flat-placeholder card was the ceiling; the design only "arrived" once real card art rendered in prod). Validate decisions cheaply; judge fidelity in prod.
- **The sole-committer rule applies to COWORK too, not just two Claude Code sessions (learned 2026-06-27).** While the goal-runner ran its `git stash -u` → run → `git stash pop` cycle on John's machine, Cowork was editing repo docs at the same time — and those edits got swept into the stash and reverted from the working tree (the failed Edit symptom: the file was momentarily back at its committed length). No data was lost (the pop restored them), but it caused confusion and a failed edit. **Rule:** when Claude Code or the goal-runner is actively mutating the working tree (stash / commit / reset / clean), Cowork PAUSES repo writes. A `git push` is tree-safe (it doesn't touch uncommitted files), so editing during a push is fine; stash/commit/reset/clean are not. Safer still: route doc changes through the queue/Claude Code, or wait for the tree to be idle.
- **Measure the bottleneck before agreeing to re-architect (learned 2026-06-28).** John (rightly) proposed static base pages to fix flat SEO, and Cowork initially championed the full static/ISR rebuild. But the goal's P0 MEASUREMENT flipped the call: GSC showed a pure crawl throttle (16 indexed / 1,007 "Discovered – not indexed" / **0** crawled-not-indexed → no quality problem), and a timed crawl showed the card page's evergreen render was ALREADY fast (baked in-memory snapshot, no network) — the entire 37.9s TTFB came from the live eBay/PokeTrace blocks on the server path. So the fix was just to move those live blocks client-side (Option B, force-dynamic-but-fast), which lifts the throttle WITHOUT reopening the ADR-047 `searchParams` 500 landmine the full rebuild would have. **Lesson:** when the question is "should we re-architect," make the goal MEASURE the actual bottleneck first — it routinely reveals a smaller, lower-risk fix than the full rebuild, and it's honest evidence over architectural intuition (even the founder's, even Cowork's). Update the recommendation on the evidence; don't anchor on what you said an hour ago. (The full static/ISR remains a valid *later* optimization, not a prerequisite to the fix.)

- **The biggest recurring trap: building infrastructure AHEAD of audience (learned across the whole 2026-06-29 session).** In one day Foil's newsletter went never-sent → live + editorial + hardened + attributed — genuinely excellent work — for **~0 real subscribers**. The honest pattern: we reached for the next *buildable* thing (newsletter, attribution, channel policy, Postiz) every time, because building is concrete and controllable, while the real bottleneck (getting real humans in front of it) is uncomfortable founder-distribution work. **Rules that fell out of it:** (1) The bottleneck at this stage is AUDIENCE/validation, not build quality — measure the scoreboard that matters (subscribers/revenue), and if a proposed build doesn't move it, it's probably procrastination. (2) "Build everything perfectly first so we never rework" (John, 2026-06-29) is a trap — you can't know what "perfect" means without users, so you perfect your *guess*, and rework anyway; it also burns the $20K/October runway. Perfect the hard-to-reverse + trust-critical (brand, data accuracy, editorial honesty); ship "good enough to learn" on everything else. (3) Paying to acquire *free* email subs is low-leverage (affiliate-cents LTV, unproven); the high-leverage thing to pay for is a *paying customer* — which needs a validated paid product first. (4) The $1K (Mercury) and any paid spend WAIT until the funnel is proven to convert organically — spending on traffic to an unvalidated funnel is the one guaranteed way to waste it. **The standing job when John reaches for another build: ask whether it produces the one missing number — proof that traffic converts — and if not, steer back to distribution + the conversion-model decision.**
- **Acquisition reality for this niche (researched 2026-06-29, cited in `docs/knowledge`-adjacent findings).** Auto-posting into communities Foil doesn't own (subreddits, third-party Discords) = bans (Reddit CQS shadowban in ~2h, proven; Discord self-bots permanent ban). SAFE full automation = OWN channels (Telegram/Discord-webhook/Bluesky/Mastodon/X-API) + paid Reddit/Meta *ads*. Owned channels RETAIN/CONVERT, they don't DISCOVER — discovery still needs founder community credibility + SEO + email. The proven collectibles playbook (PokePings/TrackaLacker model) = owned auto-fed alert channel + affiliate + premium tier; Foil's stack is ~80% of it already.

- **The human-in-the-loop firewall is VALIDATED, not theoretical (2026-06-30).** The X engagement-brief engine's very first live brief tried to cite wrong-card data ("Moonbreon" → Umbreon-ex $1,347 instead of the real swsh7-215 $2,161) to a 0-follower account. John caught + killed it before posting. This is live proof of two doctrines: (1) engagement ACTIONS on others' posts stay human-posted forever (auto-replying = platform-manipulation ban; the manual post is the ToS + brand firewall — distinct from our OWN-content auto-send like the newsletter, which is fine); (2) "ship to learn" beat "perfect first" — real use surfaced the wrong-card + empty-brief bugs that polishing never would. Accuracy fix = exact-card slug resolution, null-over-guess, never fuzzy name-match (`lib/engagement/card-resolver.ts`).
- **Watch the founder's "I'll act once the tooling is frictionless" pattern (2026-06-30).** John gated starting manual X engagement on the Discord Approve/Skip buttons being built. There's real behavioral truth (friction kills habits, so the buttons are a legit investment) — but it's also the build-instead-of-do trap wearing a UX costume. Cowork's job: build the frictionless tool fast AND keep gently surfacing that the posting itself is the irreducible growth act no tool replaces. Don't let tooling-perfection indefinitely defer the doing.
- **The autonomy-graduation answer to "when do we turn auto-posting on?" is PROOF, never a calendar date (2026-06-30).** John asked for a reminder to flip format-mining to autonomous. The right answer: don't set a date-based reminder — it contradicts the dry-run → `/approve`-gated → autonomous ladder, which advances on evidence (output quality proven + the irreversible side-effect proven). Tie the decision to the existing **Sunday acquisition review** as a tracked criterion, not a ping. AND the deeper honest point: "auto-posting" only ever means our OWN content (format-mined posts); engagement replies on others' posts stay human-posted FOREVER (ban-risk firewall). The real gate on auto-posting our own content isn't the ladder — it's AUDIENCE: auto-posting to ~4 followers moves nothing, so format-mining's near-term value is better containers for John's MANUAL posts, and full autonomy is premature until there's an audience + UTM proof that posts convert. Record graduation criteria in the brief/ROADMAP so it resurfaces every Sunday without being prematurely flipped.
- **The build-ahead-of-audience trap is sticky even when you can name it (2026-06-30, reconfirmed).** In one late session Foil shipped engagement-engine v2 LIVE *and* a format-mining engine *and* enabled its dry-run — three forward moves, all genuinely good, all respecting the autonomy ladder (v2's zero-X-write firewall; format-mining dry-run-first) — for ~0 real audience. Each step was easy to justify ("frictionless triage," "better containers," "turn it on"), which is exactly how the trap operates: building is concrete + controllable, posting/audience is uncomfortable founder work. Cowork did keep surfacing it each turn, which is the job — but the pattern persisted anyway. **Reinforced rule: at every "let's build/enable X" this stage, name the one missing number (proof traffic converts → signups) and whether X produces it; if not, say so plainly and steer to posting + the Sunday review. Closing the loop with a current brief is part of that — it makes the next session inherit the steer, not just the status.**
- **Claude Code context management (operational).** Long single sessions hit the context ceiling (saw 98%). Fixes: `/clear` between big goals, and the file+one-liner goal pattern is *designed* to keep each goal context-light (the goal file + docs carry state, not chat history). Also: Pro/Max plans support a **1M-token** window in Claude Code but it is NOT automatic — must opt in (e.g. `/extra-usage`); a default 200K is likely why a window fills fast. So a context squeeze is usually a free fix (enable 1M + /clear), not a plan upgrade.

- **The vending pivot did NOT harm the domain's SEO — measured, stop re-litigating it (2026-06-30).** John feared the vending→deal-finder pivot "screwed" foiltcg.com and asked about resubmitting a sitemap. Pulled the real GSC data (Performance→Pages, 3mo) before advising: **all ~19 pages getting impressions are Pokémon/deal-finder** (blog card-value/condition posts, the SEO pillars, `/cards` pages) — **ZERO vending pages** (`/host`, `/service-areas`, `/machines`) get any impressions. Google already reads the domain unambiguously as a Pokémon card resource; the vending pages are inert, not diluting. Two durable lessons: (1) the "pivot screwed our SEO" fear is empirically false — don't re-open it. (2) **A sitemap resubmit does NOT fix topical confusion** — it's a discovery/crawl hint, not a re-ranking lever, and the sitemap is already comprehensive. The real (smaller) SEO constraint is the young-domain crawl throttle + volume + time. And the meta-lesson (again): when John proposes a "big overhaul," MEASURE the premise first — it was false here and saved the project.
- **A live rendered-surface walk catches a whole bug class the passing test suite can't — make it periodic (2026-06-30).** Both the blank hero/founder image AND blank OG/Twitter share cards on 14 pages shipped GREEN through the full 1300+ test suite, because those tests check markup/tokens, not RENDERED reality. A 2-minute Chrome screenshot of foiltcg.com found the hero; three parallel read-only audit agents (rendering/assets · affiliate/data-trust · routes/links/metadata) found the OG blanks + a wishlist currency false-alert + the `/cards/[slug]` flaky-CDN dependency. This is the concrete case for the `design-review-loop` screenshot gate. Doctrine: periodically WALK the live conversion surfaces (Chrome render + fetch the meta tags); never treat green tests as "it works for a visitor" (extends R-015 / ADR-049). Parallel read-only audit agents are a cheap, high-yield periodic practice.
- **X reach mechanics at low-follower count (2026-06-30).** At ~4 followers, John's OWN posts reach almost no one; his **REPLIES are the discovery lever** — they surface under bigger accounts' posts, in front of THEIR audiences. So growth = thoughtful, data-cited replies on high-reach posts (the engagement engine ranks by reach), spaced out (not bursts). Original posts still matter — they're the profile a visitor JUDGES after a reply earns the click — but they don't distribute. **Stop mass-following** (John was 69 following / 4 followers; that ratio is a spam signal that can deepen a new-account throttle — let followers come from replies). And **logged-out/incognito "no posts" = X's login-wall gating on a young account, NOT a confirmed shadowban**; you cannot cleanly diagnose a throttle and the right actions (quality + patience + no spam signals) are identical either way, so don't rabbit-hole on the throttle question.
- **A goal isn't "done" until the commit (with the SESSION-LOG entry) lands — not when gates pass (2026-06-30).** The hero goal reported "Goal achieved" after gates + `/security-review` but BEFORE its conventional commit, leaving the fix sitting UNCOMMITTED in the working tree — where the next goal's `git stash -u` would have swept it. Cowork caught it by checking `git log`/on-disk state (the fix was on disk, HEAD hadn't moved). Until the AGENTS.md closure contract is tightened to make the commit-with-log the literal last step, **spot-check that HEAD moved after a goal claims done, before firing the next.** And **push each completed goal before firing the next** (John's own rule, validated) so commits don't stack. Sandbox caveat reconfirmed: line-ending churn makes `git status` show the whole repo modified — unreliable for enumerating real changes; but `git log`/HEAD and a targeted grep for the edit on disk ARE reliable.

- **"Should we rebuild from scratch?" was asked and answered with evidence — don't relitigate without new evidence (2026-07-01, Fable-5 audit).** John proposed a ground-up rebuild when Fable 5 returned. The six-branch audit (docs/AUDIT-2026-07-01-FABLE.md) showed the ARCHITECTURE is the strongest layer (code/ops B-, explicit "genuinely good" marks on the affiliate boundary, resolver, fallback, link architecture, gates) while every 🔴 was wiring/content/copy — days-scale surgical fixes. A rebuild throws away the 88-ADR lesson corpus baked into the code and reintroduces solved bug classes; and if model access is a finite window, mid-rebuild is the worst state to be caught in. Doctrine: **rebuild-from-the-goal is reserved for subsystems that fail at the DESIGN level** (2026-07: the alert engine's threshold poller, the bake/render pipeline) — everything else gets fixed in place. This is "measure before re-architect" at max scale; third confirmation of the pattern.
- **The bake-script parser drift is the SECOND writer/reader-split incident (2026-07-01; first was R-015).** `scripts/bake-card-metadata.ts` carried a stale duplicate `parseCard` → every committed snapshot since ≥May 30 had ZERO prices for every card, invisible because `tsconfig.json` excludes `scripts/` and no test asserts bake OUTPUT shape. Rules: never duplicate a parser (import + overlay with explicit baked-only-field exclusion); any script that writes a committed runtime artifact needs an output-invariant test + typecheck coverage. Promote to ADR per the PATTERNS two-instance rule.
- **Stale IDEAS/RISKS entries propagate into specs if not re-verified against the LIVE state (2026-07-01).** Cowork spec'd the logo goal on IDEAS #295's "the live mark is a literal Pokéball" — but ADR-055 had already retired the Pokéball weeks earlier; the goal-runner's P0 premise check caught it, not Cowork. Same class as the PokeTrace "renewed+paid" mistake (recorded John's words, contradicted by the Paddle portal the next day: it was scheduled-to-cancel, he clicked Don't-cancel, $98/mo continues). Rule: before citing a second-brain FACT as a goal's forcing function, spot-check it against the live artifact (site, dashboard, repo) — the second brain records what WAS true; specs need what IS true. Corollary: mark superseded IDEAS entries when ADRs resolve them.
- **The founder-pride gate (2026-07-01).** John looked at the live homepage the night before the eve send and said he was underwhelmed — so the design overhaul jumped AHEAD of the delivery. Right call, and general: a flagship distribution moment doesn't ship on a surface the founder isn't proud of, because (a) he won't push it with conviction and (b) the first impression IS the play. Distinct from perfectionism-as-procrastination: this is one bounded overhaul goal already specced, with a hard exit (the send), not an open-ended polish loop. Watch that it STAYS bounded — if the overhaul spawns a second polish round before the send, that's the trap re-forming; name it.
- **Token-allocation doctrine (John, 2026-07-01, on the $200/mo plan):** Fable = judgment/design/specs/audits, never bulk execution; Claude Code = execution from goal files; research agents staggered 2–3 max. The six-wide parallel audit worked but burned ~800K tokens in one shot and tripped the session cap mid-report — stagger next time.

## Where the current state lives (read on every session start)
1. **`docs/NEXT-SESSION-BRIEF.md`** — the latest handoff: what just shipped + the prioritized plan. THE current-state doc. **Keep it current at the end of each working session.**
2. **`docs/SESSION-LOG.md`** (latest entry) — what happened last.
3. `docs/ROADMAP.md` (backlog) · `docs/STRATEGY-*.md` (strategy) · `docs/knowledge/` (operating playbooks).

## Standing watch-items (verify against NEXT-SESSION-BRIEF, which is authoritative)
- ⏰ **PokeTrace re-subscribe (~July 15)** — load-bearing: the entire insight engine runs on it. Reminder scheduled July 13.
- Dead Supabase PAT to regenerate; CDTFA seller's permit to finish (entity # in hand); the deal-finder/email north star over the vending secondary track.

## LEARNINGS — 2026-07-03 (the belt-and-trust day)
1. **Verify the WINDOW a stat claims, not just the value.** The Dialga "n=63 · 30-day" junk was OUR mislabel of PokeTrace semantics (saleCount = all-time; avg30d anchors to the tier's lastUpdated, not today). Before rendering any aggregate, confirm what time window and population the upstream field actually describes. A correct number under a wrong label is a lie we authored.
2. **Magnitude is not incoherence.** In a top-5-chase-per-set catalog, $24k PSA 10s and 100x+ graded/raw multipliers are legitimate; junk announces itself through INTERNAL inconsistency (non-monotonic condition ladders, impossible volume for the liquidity class, same-page cross-source contradictions). John's market knowledge is a calibration input — ask him before setting thresholds on collector-market data.
3. **Pre-diagnosis pays.** Tracing the $57 alert to identity.ts's number-absence tolerance BEFORE writing the goal let Claude Code confirm-in-code instead of exploring; the fixture matched the prediction. Reading the admission path first is cheaper than a discovery phase.
4. **docs/goals/QUEUE.md is now the goal tree** (statuses: PENDING/IN-FLIGHT/CLOSED-UNPUSHED/LIVE/GATED). Cowork adds rows when writing specs; Claude Code flips statuses at closure. The brief carries strategy; the queue carries the tree. Neither brain re-derives "what's next" from memory again.
5. **Loops need a judge before they earn a night.** Twice today the right answer to "run it as a loop?" was no: deterministic edits (hero-simplify) and one-page copy (GitHub README) don't need iteration machinery, and the judge harness that would make overnight loops trustworthy still isn't built. The harness stays a prerequisite, not a nice-to-have.
6. **Positioning follows provable capability.** Manipulation-protection stays a content lane + staged detection ladder until we can demonstrate detection; an unfalsifiable promise ("we protect you") violates lead-with-proof even when the trend is hot. Same rule as the flywheel: capability first, claim on proof.
7. **Voice rule extended:** the site speaks first-person singular now ("Tell me the cards you're chasing") — "us" reads corporate for a solo builder whose X voice says "I'll watch it for you." One voice across site, email, X. And John's spit-take on the vault tweet draft re-taught the deeper rule: parallel-structure triplets and "what's in yours?" engagement-formula cadence read as corporate speak even with correct vocabulary.
p and failed the deploy. Fix: `outputFileTracingExcludes`. Watch for any in-tree artifact directory riding into function bundles.
- **John's voice rules (bank these; guards exist in email tests, extend everywhere):** "chasing," never "hunting" · **NO EM DASHES** in X/email voice (periods and commas do the work) · plain collector words · deadpan-with-warmth, effort worn lightly ("ok I may have overdone it") · honesty as flex ("I'd rather show nothing than a made-up number" — his favorite line of the day) · gift restraint: deliver then speak, never tease ("done" beats "coming soon"); don't announce the dedication chip, let it be found.
- **Persona lensing is standing practice (John, 2026-07-02):** every goal spec / research task / review names the top-tier professional lens it's held to (design engineer, growth operator, deliverability specialist…) at the top of the artifact. John also wants it in the Project Instructions (handed him the paste).
- **Ergonomics before autonomy when the strategy makes JOHN the engine.** He called it directly: if his job is being in the replies, Cowork's job is his workflow's friction first (in-flow receipts: bookmarklet + iOS shortcut + /api/receipts), machine autonomy second. Ratified strategy spine: zero-input flywheel as the horizon; capability built now, autonomy flipped on PROOF; conversion before throughput (automation amplifies whatever loop it's pointed at — automating an unconverting loop is spam at scale); the cold X lane stays human FOREVER (ToS wall = the channel's license fee); monetization decisions wait for engaged users (affiliate already passive).
- **Email brand = voice, not pixels (ADR-079 reaffirmed under pressure).** John proposed the pretty banners in emails; held the line — images/buttons are the Promotions signals, and his own "clean" verdict on the imageless vault email proved the point. Revisit is test-gated: banner-vs-text A/B on fresh Gmail accounts at ~500+ subs, tab placement decides. Beehiiv operational nuance: the email THEME masthead is separate from the publication logo (Settings) — theme edits are editor+publish, John's clicks.
- **The file+one-liner architecture is antifragile — validated twice in one night.** A 100%-context stall mid-goal and a literal power outage both cost ~zero: specs on disk + commits + a fresh `/clear` resumed cleanly each time (the re-run even caught 4 real bugs the stalled session had staged). Corollaries: fresh `/clear` per big goal by default; stale `.git/index.lock` after a crash blocks everything until deleted; a paused-mid-write goal must resume and commit before another goal touches the tree (interrupt is only clean if nothing was written).
- **X-content mandate (John, 2026-07-03 ~00:00): Cowork LEADS content strategy — formats, hooks, timing, research, every first draft (each with its mechanism stated: what format, why this hook, what it should produce). John keeps the voice veto + the send — empirically where he won all day (chasing, no-em-dash, the natural bio, "took me a couple days but"). Rule: Cowork originates, John vetoes; never send around him, never make him blank-page under pressure. Expertise claim stays honest: researched playbooks + measurement, formats earn their slot by measured card-name replies/signups and get killed publicly when they underperform. The replicable eve-pattern reply playbook (find fresh price-pain/grail posts → verified sold-comp value first → seller-brain read → LINKLESS offer "I'll set a watch, takes me 2 min" → profile/pin does the selling → every yes = a same-day delivery clock) is the standing cold-session format.**
- **Screenshot-only = call the play worked all day; the unsent-buffer rule held** (multiple consequential commands sat unsent on the input line — always assumed unsent, always right). Also: when a runner mid-goal makes a BRAND decision on its own (the sakura-petal favicon replacing the hanko), surface it for John's explicit ratify/reverse — it may be right (it was), but silent drift and deliberate succession must stay distinguishable.

## LEARNINGS — 2026-07-05 (the strategic reframe: validate willingness-to-pay, and stress-test EVERYTHING)
1. **The 14-month pattern, finally named and being fixed: Foil has been four products (scanner → vending → deal-finder → market-insight board) and never once tested whether anyone WANTS it enough to ACT.** The entire strategy rests on invented conversion numbers (15–25% signup, 70× LTV) never validated against real traffic (GSC: 0 clicks, ~0 real subs). **The reframe (John, 2026-07-05): stop building, VALIDATE.** Retire the `/deals` board as the hero (it's supply-constrained — 0–6 real deals/day, a weakness on a webpage but a FEATURE in an inbox), turn the deals into a **gated daily deal drop** (top 2 public, rest email-gated), add a **$6/mo paid tier (1-mo free via Stripe)** for personalized event-driven price alerts, and run a **bounded ads test** that measures willingness to PAY. Plan: `docs/goals/foil-gated-drop-paid-test.md`.
2. **Generic-daily vs personalized — the WTP refinement (John's intuition, 2026-07-05, has real grounding).** Thin supply (0–6/day across the WHOLE catalog) cuts AGAINST personalized as the PAID draw: an individual's watched cards get a hit only ~every 1–4 weeks → personalized alerts are LOW-FREQUENCY → empty-inbox churn ("what am I paying for?"). The generic daily drop ALWAYS delivers + builds a daily habit (the recurring-charge justifier). Counter-evidence: the proven PAID monitoring products ARE personalized (Keepa, CamelCamelCamel premium), while generic deal aggregation is usually FREE/ad-supported (Slickdeals, r/PkmnTCGDeals) — so generic-as-paid fights a free default. They win on different axes: **generic = the buy trigger (frequency/habit); personalized = stickiness (switching-cost + the grail-hits-price moment).** Resolution: the **$6 bundles BOTH — the daily drop as the primary paid draw (John's lean) + personal watches as the retention lock-in; free = the 2-deal teaser.** And because these are WTP intuitions (untrustworthy by our own rule #3), the free/paid line + which HOOK sells is an **A/B in the ads run** (same $6, two hooks: "today's best deals, daily" vs "watch your grail, ping at your price"). Don't settle it by argument — test it.
3. **Ads are the VALIDATION instrument, not a scale lever — Cowork was too purist, John corrected it (bank this).** You cannot validate a funnel without eyes; organic eyes are authority-gated (months) or human-bound (X replies). A SMALL bounded ads test BUYS the validation data — that is not "spending on an unvalidated funnel," it's buying the answer to the only question that matters. A card-entered trial >> a free email as a desire signal (conviction vs cheap). Precondition: know the ONE offer being tested.
4. **STRESS-TEST every idea and logic path BEFORE executing (John, standing rule, 2026-07-05).** The ranking-rework premise check is the model: it caught the empty-board trap (hard $-floor + exclude-vintage → 0–3 cards/day) BEFORE any code shipped, and surfaced real customer-facing TRUST bugs (a stale ~2× card-page price on Neo Umbreon; the inflated TCGplayer $229 on the variants panel/`/lines`). Adversarial premise checks are now doctrine, not optional. **Trust bugs are pre-ads BLOCKERS** — a wrong number shown to a paid-trial user burns the whole test.
5. **The board is retired as the hero; the honest-price ENGINE underneath survives.** What wowed @mollipen tonight (a real, trustworthy "what's it actually worth," sold-not-asking, confidence-gated) is the desirable core — not a board of $1 movers. Kill the always-on board as the front door; the value moves into the gated email drop. `deals-ranking-rework` is PARKED (its premise-check findings in `_results/deals-ranking-rework-premise-check.md` already delivered the decisive "retire it" verdict — do NOT ship its ranking code).
6. **Session discipline note:** the highest-value act of the whole session was a distribution one (the @mollipen DM), not a build. Every time Cowork or John reached for a build (100/100 pages, every-card-listed, more movers), the honest answer was "that doesn't produce the one missing number." The reframe finally points all the compute at that number: does anyone pay?

7. **High-stakes, judgment-heavy initiatives get the JUDGMENT SEAT (Fable) on a FRESH surface — don't pre-spec them with Opus/Cowork (John, 2026-07-05).** John held the ENTIRE validation initiative until Fable returns (post-Jul 7) rather than let it be pre-planned tonight, and told Cowork explicitly NOT to pre-spec it. The rule: when a move spends real money or hinges on judgment (pricing, ICP, a strategy pivot), a detailed Opus/Cowork pre-spec PRE-CONSTRAINS the judgment seat. **Capture the FACTS learned; leave the DIRECTION un-spec'd so Fable plans it fresh. Preserve context, not conclusions.** The two planning docs were reframed to "raw context, not a spec" accordingly; the pre-launch map in the brief is REFERENCE, not a to-do.

## LEARNINGS — 2026-07-12 (the stranger-run night: the funnel walked like a paying stranger; five bugs the tests couldn't see die)

1. **THE STRANGER RUN IS NOW DOCTRINE.** Green tests + green closure gates ≠ a walkable funnel. Fable driving John's browser through the LIVE money path (guest checkout → email → sign-in → entitlement → watch → cancel) caught in ONE night: dead guest sign-in links (implicit-flow tokens the SSR callback can't consume), Supabase's built-in mailer silently 503ing ALL magic links (2/hr courtesy cap — two signups exhausted it), swallowed /login errors, invisible-ink inputs (dark-OS variants vs the forced-cream page), and the checkout brand-void. Extends the 06-30 live-surface-walk doctrine to the FULL transactional path. **Standing rule: before any paid-traffic moment, someone who is not the builder walks the whole funnel on prod with a real card. John's bar: if Cowork can drive it end-to-end, a non-tech user can.**
2. **Supabase Auth sends its OWN email** — it never touches our Resend code until custom SMTP is wired into its auth config. Now on Resend SMTP from `alerts@foiltcg.com` (ADR-114). **Coupling banked: rotating `RESEND_API_KEY` requires re-PATCHing Supabase's `smtp_pass`** (ENV-VARS row) — a key rotation that forgets this silently kills every sign-in.
3. **/security-review caught a REAL open redirect in code written the same hour** (`next=/%09evil.com` — the WHATWG parser strips tab/CR/LF BEFORE resolving, so string-prefix checks are bypassable; the fix resolves against a sentinel origin and demands same-origin; vectors regression-pinned). Adversarial review of just-written security code is where the gate earns its cost.
4. **Test-mode Stripe events never reach the prod webhook → entitlement drift.** John's own account sat at tier=pro off a canceled test sub, holding a test-mode customer id that would have broken his next LIVE upgrade ("No such customer"). Rule: any test-mode Stripe mutation gets a manual entitlement reconcile; never let a test-mode customer id persist on a live row.
5. **The email-typo lockout is real** — John typo'd `.co` on run #1, paid, link went nowhere. Mitigation queued (success page shows the entered address); the guest recovery path is /login resend, which now works and says so.
6. **John's taste call, banked as a standing design lens: joy surfaces get judged on emotional register, not just conversion mechanics.** Fable graded /start "clean, honest" and missed that naming your grails — the most dopamine-loaded moment in collecting — was dressed as a form. Foil-register delight (V6.6 spec): card-art pack-reveal, REAL sold-data as the payoff (truth is our slot machine), a growing binder, one lineage next-add. Never streaks/confetti/urgency — dark patterns burn the trust moat. **Add a "collector-joy check" to every design rubric beside the glance test.**
7. **Stripe's cancel modal defaults to period-end and users misremember their click** (John was "pretty sure" he picked immediate; his own screenshot + the live entitlement both said scheduled). Product consequence: /account must render scheduled-cancel truthfully ("Pro until X · won't renew") — "next charge X" shown to a canceling user is a false claim (V6.5 ledger). Verify billing states from the surface, never recollection.
8. **The judgment-seat boundary held under momentum:** "write the ads-run spec" nearly went to Claude Code at 1 a.m. Strategy/judgment artifacts (ads spec, pricing, ICP) are authored in the Fable seat; executors execute. Corollary: judgment work gets MORNING hours, not the tail of a six-hour high.

## LEARNINGS — 2026-07-04 (brand ships; the data spine's real disease is named)
1. **The load-bearing data doctrine (John's domain knowledge — bank this above almost everything on the data layer):** TCGplayer's "market price" FREEZES when a card has no active listings — it stops updating rather than reflecting reality. PokeTrace leans on that TCGplayer market price, so **Foil's entire insight engine has been pricing off a stale passthrough that lies by omission.** This is the likely single root cause of BOTH the week-frozen `/deals` board AND the recurring sold-data-coherence bugs — we kept writing goals that "didn't understand the data" because the data itself was silently stale. **Fix direction: price off ACTIVE-LISTING reality** — eBay lowest active listed (already in-stack via eBay Browse — the card-page/`/go` path), and/or PokeTrace's eBay-sourced figure over the TCGplayer number. This is the **Card Ladder** model (collectors' real source of truth alongside PSA-adjacent tools like Collectr). Likely a BASIS change, not a new vendor — which also weakens the ~Jul 15 PokeTrace $98/mo renewal case. **And the deeper framing (John): we are NOT after a single 100%-true price. Collector prices are inherently uncertain — the honest output is a confidence-gated READ (estimate + how sure + why), never false precision.** Same DNA as null-over-guess and the coherence gate. The `deals-freshness-diagnosis` goal (full-catalog scan → Chrome walk of TCGplayer's live listings on every suspect → eBay-lowest gap) is the diagnose-first instrument; it must produce a ranked cause table, not a silver bullet. The #errors "sold-data suppressed" pings are the live incoherence map, not noise.
2. **Do NOT run bulk EXECUTION on Fable — it drained the entire weekly cap in ONE day (2026-07-04).** A full day of goals plus x-reply-desk at xhigh effort (~250K tokens) exhausted Fable's 50%-weekly promo allowance; by evening BOTH the Cowork advisory seat AND Claude Code were walled ("out of usage credits"). The token doctrine held all along and today proved the failure mode: **Fable = the advisory/decision seat + genuine synthesis ONLY; Opus 4.8 = Claude Code execution + all mechanical/bulk work** (Chrome walking, catalog queries, scaffolding, long goals). Opus is fully capable of rigorous diagnosis — reserving Fable for "the thinking" is not a downgrade. **The blocker when Fable's capped is usually a SAVED-DEFAULT setting, not a real limit:** Claude Code silently saved its default to Fable, so every new goal hit the wall — the fix is `/model` → Opus, not waiting for credits. Fable promo = 50%-weekly until Jul 7, changes after (verify what it becomes). Practical rule: keep Claude Code's default on Opus; switch to Fable only for a specific judgment goal, then switch back.
3. **SEO indexing is AUTHORITY-gated, not defect-gated — do NOT build more pages to fix it (2026-07-04, seo-crawl-hygiene).** The real content pages (pillars, blog, cards) sit in GSC "crawled – currently not indexed" because the DOMAIN lacks authority (~1yr old, ~0 backlinks), NOT because of any on-page problem — proven by desktop Lighthouse SEO 100 / LCP 1.4s + live-validated clean JSON-LD (Product+Breadcrumb on cards, Article+FAQPage on blog/pillars). Google crawls them fine and chooses not to index yet; that's a trust signal. **The lever is off-page: backlinks, referral traffic, being cited** (the X reply engine, the eve/creator strand, the newsletter) — not more code, not more content pages (more thin-to-Google pages on a low-authority domain dilute, not add, authority). The sitemap already carries 2,079 real content URLs; a resubmit is a discovery hint, not a re-ranking lever (consistent with the 2026-06-30 vending-SEO finding above). GSC "known pages" (the 1,007→56 swing John saw) is Google's fluctuating *discovery* count in a property view, not the submitted-URL count — a report artifact, not a defect. Cheap plumbing shipped alongside (llms.txt created + allowlisted — it was 307ing to /login; /login noindexed) but those don't move indexing; time + authority does. Third confirmation of "measure the premise before the big overhaul."
3. **Two standing-rule violations in one session, both corrected — re-internalized:** (a) sent John a wall-of-text Claude Code prompt instead of the file+one-liner (he was sharp about it — "the fable guy got up to speed instantly"); (b) wrote three goal specs without adding QUEUE.md rows in the same edit (QUEUE's own rule). Both are non-negotiable: **every spec gets its QUEUE row in the same beat, and Claude Code always gets a file + one-line `/goal …`, never a pasted wall.** The receipts/ship/security specs were all reduced to one-liners after the correction. These aren't preferences — they're how this operator works, and missing them reads as not having read the contract.
4. **"Reply right now" ≠ flip the API-post switch.** John wanted his X write creds "turned on" to start posting. Correction that held: the X write creds ARE set (configured 06-26, in `.env.local` + Vercel prod); what's off is the LIVE switch, deliberately. He doesn't NEED it — the receipts tool (intent-link → X composer → he presses Post) IS the reply-right-now path, human-posted, zero ban risk. The API-post Approve lane is only for user-initiated contact (near-zero inbound at ~4 followers) and gets ONE supervised test before trust. Auto-posting on the cold lane stays banned forever (the firewall). So "make posting frictionless" = ship the receipts tool, not flip a switch.
5. **NEVER infer live-surface data — PULL it. And set up the API so it's automatable (John, 2026-07-04, emphatic — "do not make this mistake again").** The seo-crawl-hygiene goal "reconciled" the GSC "56 indexed vs 2,079 sitemap URLs" question by INFERENCE (counting the sitemap) because no GSC creds existed — an assumption on the load-bearing "why aren't we indexed" question. Same class as the eve-strand-inference and the PokeTrace "renewed+paid" mistakes. **Rule: for any live-surface fact (GSC index status, X thread state, a dashboard, a plan/tier), pull it via API or Chrome — never reason it from a proxy signal.** GSC has a FREE API (`searchconsole.googleapis.com`: URL Inspection = per-URL index status + why-not-indexed; Search Analytics; Sitemaps) — set up a service account + grant it property access so future SEO work is grounded and automatable (`gsc-api-integration.md`). Generalize: when a goal needs live data and lacks credentials, the right move is to SURFACE that and set up access, not to infer around it.
6. **Public/brand copy is John's VOICE VETO — a goal must not ship it autonomously (2026-07-04).** The seo-crawl-hygiene goal wrote the customer-facing `public/llms.txt` brand copy ("Foil is a Pokémon TCG deal-finder for collectors…") and committed it without John's review. Per the X-content mandate: Cowork/Claude Code ORIGINATE copy, John vetoes voice, nothing customer-facing ships around him. Goals that generate public copy must route it to John for veto (or clearly flag it unpushed), never treat it as closed.
7. **Judge site health on the form factor that matters — MOBILE for X traffic (2026-07-04).** Cowork called the PageSpeed results "polish" off the DESKTOP score (96/LCP 1.4s). John pulled the MOBILE report: Perf 67 / **LCP 8.3s** — because the hero belt loads all 8+ faces `eager` (~600KB) and saturates Slow 4G. X clicks are mobile, so an 8s blank page is very plausibly WHY replies don't convert. Lesson: X-traffic quality gates convert on MOBILE; always check the mobile PageSpeed before judging perf, and treat mobile LCP as a conversion metric, not vanity. (`homepage-mobile-perf.md` — eager only the visible belt faces.)
8. **STANDING DIRECTIVE — steer John OFF screenshots (John, 2026-07-04, "a bad habit I need to break").** Cowork now has DIRECT read access to most of what John was pasting: the repo/git/goal `_results` via file tools; the **TERMINAL + DISCORD via computer-use** (granted 2026-07-04 — `Windows PowerShell` at click-tier read-only, on the **ZOWIE XL2546X** monitor, `switch_display` to it; `Discord` at full-tier); GSC/dashboards via API once wired; web/dashboards via Chrome. **RULE: whenever John pastes a screenshot of something Cowork can read directly, give a one-line nudge ("I can read that myself now — just ask 'how's the goal' / 'what's in #content-engine'") AND then read it directly.** Keep it a light reminder, never preachy; the aim is zero screenshot-pasting. To check the Claude Code goal: `switch_display` ZOWIE → `screenshot`. To check Discord: `open_application` Discord → `screenshot`. Repo/commit questions need no screen at all — read git + files.
9. **GSC is now WIRED — the indexed number is LIVE, not inferred (2026-07-04, closes #5; confirms #3 with data).** The Search Console API is live (`lib/seo/gsc.ts` + `scripts/gsc-index-report.ts`, [ADR-109](DECISIONS.md#adr-109--google-search-console-api-client-live-index-coverage-over-inference); SA `gsc-reader@n8n-content-project.iam.gserviceaccount.com` has Full access; key base64 in `.env.local`, never logged). **The real numbers (2026-07-04, 28d window):** sitemap submits **2,079** URLs; only **17 pages surfaced in search** (609 impressions, **0 clicks**); an inspection sample of 45 = **1 indexed / 12 "Crawled – currently not indexed" / 32 "URL is unknown to Google."** This **confirms the authority thesis (#3), not a defect:** every pillar + blog post is crawled-not-indexed with robots ALLOWED + self-canonical (no on-page block — Google is choosing not to index), and ~71% of the sampled catalog is **undiscovered ("unknown to Google")**. So the SEO constraint is **discovery + authority (backlinks, referral traffic, being cited via the X/eve/newsletter strands), NOT more pages and NOT a sitemap fix.** Re-run `node --experimental-strip-types scripts/gsc-index-report.ts` any time for a fresh `docs/goals/_results/gsc-index-report.md` — no more inferring the index number. **Two zeros worth sitting with: 0 clicks in 28 days, and only 17 of 2,079 pages ever shown.**
10. **The GSC verdict's OPERATING consequence + two follow-ons (2026-07-04).** (a) **Steer: stop building for SEO — the lever is distribution.** With 0 defects and the gate being discovery/authority, more pages or on-page work is proven wasted; the ONLY mover is off-page (John in the replies, newsletter, backlinks/citations). This is the 4th confirmation of "measure the premise before the overhaul" — now with the actual instrument (the free GSC API) John asked for, so future SEO questions are PULLED, never inferred. (b) **Phase 3 `gsc-index-manager` is a deliberate LATER/low-pri MONITOR, not a fixer.** John rightly asked for the report to be actionable by an owner (Cowork), and it will be — BUT the n=45 data shows nothing to auto-fix, so it's a regression-alarm + authority-trend tracker + judicious ≤10/day priority-page Request-Indexing (UI-only, over Chrome — there is NO API to force-index our page types; the Indexing API is JobPosting-only). Do NOT build it expecting it to move indexing; it can't. (c) **EXTERNAL STATE to track — GCP billing risk.** The `n8n-content-project` GCP project (which hosts the GSC service account we now depend on) is flagged **at risk of suspension** — billing account `01CBF6-7D6D18-F894B6` is past due / invalid card (3 Google emails 2026-07-04 17:47Z; the account had auto-upgraded to paid). If suspended, the SA/API access breaks. **John must fix the card at `console.cloud.google.com/billing/01CBF6-7D6D18-F894B6/settings`** (billing = his gate). A separate Google Workspace invoice for foiltcg.com also auto-charges — likely the same card. Also note (good): GSC emailed 07-03 that a "Page with redirect" indexing issue was VALIDATED as fixed (the llms.txt 307→/login + /login-noindex work) — so the last real defect is already resolved, consistent with the 0-defect scan.


## LEARNINGS — 2026-07-11 (the offer-lock session; bridge restored after 3 dark days)
1. **THE OFFER IS LOCKED — decisions of record live in `docs/goals/offer-implementation.md` (gitignored; re-read before touching any offer surface).** Consumer $6 test · modern-chaser ICP, grail hook · limits model (free: teaser + weekly digest + 3 daily-checked watches, gift vaults exempt; Pro: unlimited hourly-checked watches + the daily drop) · honest founding rate-lock (NO unshipped artifacts in copy — John's own "app coming soon" idea vetoed by his done-beats-coming-soon rule, and he ratified the veto) · Reddit+Meta 50/50, two hooks, ~$300, per-cell consolidation rule · pre-ads gates: return-to-checkout + one-trial-per-customer.
2. **TWO-VOICE ARCHITECTURE (deliberate succession of the 2026-07-03 first-person rule, NOT drift):** product surfaces (site, alerts, transactional email, /pro, the drop) speak as **Foil the agent** — the watching IS automated, so agent-voice is MORE honest than "I" there. John's "I" stays on X + editorial newsletter segments (human credibility + the ToS firewall). Core marketing line, ratified verbatim: **"Foil doesn't guess prices. It reads real sales."** Avoid the literal word "AI" on product surfaces (collectors read it as hallucinated prices); Foil is "an agent that watches the market," always anchored to sold data.
3. **REGISTER RULE (John, ratified — all customer-facing copy):** card-shop language. Collector words fine (grail, chase, NM); finance/tech words banned (aggregate, windowed, sample-size gated, momentum, basis). Test: a 15-year-old collector gets it on first read. Model line already live: "~$16.43 this week, usually $19.11." The newsletter playbook backs it (operators write at grade 5–7 as the differentiator).
4. **Scope-honesty, 2nd instance (after Dialga): a thresholded top-list is NOT a market sample.** Cowork drafted "12 of my 20 movers are below average" — those 20 are the tails that crossed thresholds; the line implies a market split the data can't support. John caught it. Rule: NO market-level claims until the **market-temperature stat** exists (share of the FULL tracked universe below its own 30-day average — spec'd in offer-implementation). The cooling/correction narrative (externally real, July 2026) lives in ads/drop/X only — never evergreen site copy, never predictions, never blog-quoted numbers (a viral "$800 Moonbreon" claim contradicted our fresh windowed $1,523 on n=321).
5. **Walkthroughs default to CLI GOALS, not manual dashboard playbooks (John, emphatic).** Cowork hand-walked the Vercel env UI when the routing rule says `vercel env` via a goal. The working pattern: `stripe-live-wiring.md` automates everything and PAUSES exactly at the credential moments — John runs `vercel env add <SECRET> production` interactively in HIS OWN terminal so secret values never enter any agent context. Manual clicks are for true credential/billing moments only.
6. **Leaked live secret = roll it now, debate never.** John's sk_live hit the chat via a screenshot mid-setup; rolled in 30 seconds at zero cost (nothing wired yet). PowerShell gotchas that caused it: values must be QUOTED (`$env:X="value"`) and ONE command per line — an unquoted paste executes the key as a command and prints it everywhere.
7. **Build-trap watch, named in-session:** the offer-lock generated four build ideas (speed tiers, agent dress, market-temperature, demand-driven catalog). Three were bounded into the existing goal; demand-driven catalog expansion PARKED to IDEAS — let test users bump the coverage wall first; their unmatched watches ARE the prioritized expansion list. Also parked: chat-first UI (post-validation bet). Marketing leads with the curation ("top 5 chase cards from every English set"), never a card count.
8. **GSC monitoring note:** impressions collapsed to ~0/day in early July (609→25 per 28d; 3 pages indexed; no manual action). It's the authority gate deepening — not a defect, not actionable, do NOT reopen SEO. GCP billing (`n8n-content-project`) still past-due; if it suspends, the GSC API monitoring dies (John's card fix).

## LEARNINGS — 2026-07-14 (LinkedIn/career workstream opens · sold-truth positioning · the personal-profile retarget)
1. **A new live workstream: John's CAREER / personal brand, distinct from Foil the business.** Foil is his #1 job-search PROOF, so they overlap, but personal-brand moves (LinkedIn) are *John's-profile* work, not Foil goals. **Correction banked: John has a BA in Finance; an MS in Data Science is a GOAL he does NOT hold — never state/imply he holds it on any resume/LinkedIn/copy** (I put "MS in Data Science" into a LinkedIn draft before he caught it — live instance of [[verify-johns-numbers-before-repeating]]). **Career positioning LOCKED: primary lane = Forward-Deployed Engineer / Solutions / applied-AI (operator-who-builds: enterprise sales + a solo-shipped production app); data/BI secondary.** He builds with AI agents (Claude Code) — frame plainly ("builds production software with AI agents"), never as jargon like "harness engineering." See memory `john-career-track`.
2. **LinkedIn = John's PERSONAL profile, NOT a Foil company page (the retarget lesson).** A spec written without repo access pointed at a nonexistent goal file and targeted a Foil company page; retargeted because John's audience (1,800 followers) + job-proof live on his PERSONAL profile — a 0-follower company page just relocates the "posting to nobody" problem. Phase 1 = paste-rail (caption → `#content-engine` → John posts himself); personal-profile posting stays human/manual (more authentic for proof, no LinkedIn API approval needed). ADR-117.
3. **Sold-truth positioning is the sharpened center of gravity** (`docs/STRATEGY-SOLD-TRUTH-POSITIONING.md`): "eBay shows asking prices; Foil shows what it actually sold for" — buyer-protection / anti-overpay. Confirmatory (where strategy already pointed), not a pivot. **Hard guardrails:** (a) the claim is only as true as the spine → GATED on pricing-bridge; do NOT market "source of truth on sold data" while the sold feed is frozen/lapsing (lead-with-proof). (b) "only PriceCharting existed" is an OVERCLAIM — eBay's own sold filter, 130point, Card Ladder exist; our edge is done-for-you, not "only" (never make a claim a collector debunks in 10s — trust is the whole brand). (c) scalper-villain energy in ADS/X only, precision on-site. (d) no literal "AI" on price surfaces (reads as hallucinated). (e) not "the one true price" — confidence-gated read. **Product "aha": we already hold BOTH eBay lowest-listed AND real sold — show the listed-vs-sold contrast.**
4. **Chrome `type` drops characters on LinkedIn edit fields** (saw "Reas"/"movemets"/"auomated" from a clean-typed string). For exact copy on a live profile: zoom-verify before Save, or hand short text to John to paste. LinkedIn Featured link-preview fetch failed once ("invalid") then succeeded on retry — transient; retry before concluding a URL can't be featured. The Featured thumbnail is the site's OG image (sharpen it in-repo to improve every share, not just Featured). Extends the "hand fiddly visual edits to John" note.
5. **Don't inherit a spike memo's headline as current truth when writing the brief.** The `pricing-bridge` goal-runner's P0 check corrected TWO premises Cowork had just baked into NEXT-SESSION-BRIEF: (a) PokeTrace's sold ingest had RESUMED (the spike's "frozen at 07-05" was stale within a day — high-velocity cards now read 1–3 days old, unevenly), and (b) Track A Phase 2 (catalog automation) was ALREADY SHIPPED hours before the spike memo (the 541-card gap was already closed, daily bake cron autonomous on main). Load-bearing state — vendor freshness, what's already merged — moves faster than any memo; re-verify it LIVE before committing it to the brief. Same class as [[verify-johns-numbers-before-repeating]]. (Net-good: the goal made the lapse survivable AND the ingest came back, so the ~Jul 15 clock is de-risked and renewal is now a genuine re-decision, not a forced "don't.")
