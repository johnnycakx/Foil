# Environment Variables Registry

One row per env var. Source-of-truth for "where do I set this?" and "is it safe to log?"

When you add a new env var anywhere in the codebase, add a row here in the same commit. When you remove a var, mark it deprecated rather than deleting — old branches may still reference it.

---

## Active

| Name | Purpose | Configured at | Sensitivity | Required by |
|------|---------|---------------|-------------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL. Public — safe to embed in client bundles. | `.env.local` · Vercel (production + preview) · GitHub Actions (autonomy workflow needs it for data injection) | Public | Auth, DB queries, content engine data injection |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key. Public — RLS gates access. | `.env.local` · Vercel (production + preview) | Public | Browser auth flow |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service role — bypasses RLS. Used by webhooks + admin tasks + content engine. | `.env.local` · Vercel (production) · GitHub Actions | Secret | Stripe webhook, autonomy data injection, admin scripts |
| `ANTHROPIC_API_KEY` | Anthropic API key for Claude Vision + content engine. | `.env.local` · Vercel (production) · GitHub Actions | Secret | Vision identify/confirm/retry passes; content engine `generateWeeklyPost` |
| `POKETRACE_API_KEY` | PokeTrace pricing API key (X-API-Key header) — sold-history + price-history on `/cards/[slug]`. ⚠️ **RE-SUBSCRIBE IN PROGRESS:** the Pro tier was cancelled 2026-06-16 during the vending pivot, then the deal-finder was restored as the primary indexed surface ([ADR-064](DECISIONS.md#adr-064--dual-track-site-deal-finder-restored-as-primary-indexed-seo-surface-vending-lead-gen-kept-at-host), 2026-06-23). John is re-subscribing; **re-add a valid key to Vercel (production) + `.env.local`.** Read by `lib/poketrace/by-uuid.ts`, `lib/poketrace/price-history.ts`, `lib/poketrace.ts`. **Soft-fail safe:** when the key is missing/invalid, those fetchers early-return `null` and the card page renders **without pricing, never a 500, and still indexable** — so a lapse never costs SEO. (PriceCharting, a separate pricing reference and never an env var here, was cancelled at the same time and is also being re-subscribed.) | `.env.local` · Vercel (production) | Secret | `/cards/[slug]` sold-history + price-history (soft-fails when absent) |
| `STRIPE_SECRET_KEY` | Stripe secret key — test mode currently; switch to live post-LLC. | `.env.local` · Vercel (production) | Secret | Checkout, customer portal, webhook signature verify |
| `STRIPE_WEBHOOK_SECRET` | Stripe webhook signing secret. Verifies webhook payloads aren't forged. | `.env.local` · Vercel (production) | Secret | `app/api/webhooks/stripe` |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | Stripe publishable key for client checkout redirect. | `.env.local` · Vercel (production + preview) | Public | Client checkout |
| `NEXT_PUBLIC_SITE_URL` | Canonical site URL for absolute links (sitemap, OpenGraph, JSON-LD `@id`). | `.env.local` · Vercel (production + preview) | Public | Sitemap, JSON-LD, OG metadata |
| `BRAVE_SEARCH_API_KEY` | Brave Search API key for SERP context injection in the content engine. | `.env.local` · Vercel (production — optional) · GitHub Actions (autonomy) | Secret | `lib/seo/serp-fetch.ts`. Engine degrades gracefully without. |
| `AUTO_PUBLISH_WEEKLY_POSTS` | Kill-switch for autonomy. `"false"` reverts to `_pending/` drafts; any other value (or unset) → publish direct to `main`. | GitHub Actions repository **variable** (not secret) | Public | `scripts/generate-weekly-post.ts` |
| `WEEKLY_POST_WEBHOOK_URL` | URL POSTed to on successful publish + on gate-exhaustion failure. Free-form — Discord, Slack, Zapier, n8n. | `.env.local` · GitHub Actions (autonomy) | Secret | `scripts/generate-weekly-post.ts` |
| `VERCEL_DEPLOY_HOOK_URL` | **UNUSED as of Session 47.4 / [ADR-045](DECISIONS.md#adr-045--autonomous-commits-use-a-team-associated-author-email-so-vercel-doesnt-block-the-deploy).** Formerly POSTed by the autonomy workflow's "Trigger Vercel deploy" step (ADR-008) to force a production build. That step was removed — the bot's commits now deploy via Vercel's standard git integration once the self-defeating author-ignore build command was deleted. The secret is retained (inert) and can be revoked in the Vercel UI; nothing reads it. | GitHub Actions (no longer read) | Secret | (none — step removed) |
| `BEEHIIV_API_KEY` | Beehiiv API key for newsletter subscription writes + autonomous draft creation. Server-side only — CORS blocks browser calls (see [ADR-010](DECISIONS.md#adr-010--beehiiv-for-newsletter-list-management-official-sdk-single-field-form-server-side-key), [ADR-011](DECISIONS.md#adr-011--newsletter-drafts-auto-generated-never-auto-sent)). | `.env.local` · Vercel (production + preview + development) · GitHub Actions (autonomy workflow) | Secret | `lib/beehiiv.ts`, `lib/beehiiv-posts.ts` |
| `BEEHIIV_PUBLICATION_ID` | Beehiiv publication ID (prefixed `pub_…`). Identifies the "Foil" publication for every SDK call. | `.env.local` · Vercel (production + preview + development) · GitHub Actions (autonomy workflow) | Secret | `lib/beehiiv.ts`, `lib/beehiiv-posts.ts` |
| `RESEND_API_KEY` | Resend transactional email key. Originally landed for the manual-paste newsletter fallback (see [ADR-012](DECISIONS.md#adr-012--newsletter-manual-paste-fallback-via-email-supersedes-adr-011-api-path)); also consumed by the wishlist alert cron (ADR-024) on every threshold-meeting price drop AND the vending host-lead email ([ADR-060](DECISIONS.md#adr-060--vending-host-lead-gen-pivot)). Server-side only. Free tier: 3K/month, 100/day. | `.env.local` · Vercel (production + development) · GitHub Actions (autonomy workflow) | Secret | `lib/notifications/resend.ts`, `app/api/cron/wishlist-alerts/route.ts`, `app/actions/host-lead.ts` |
| `LEAD_NOTIFICATION_EMAIL` | Recipient for `/host` venue lead emails. **Set in production to `john@foiltcg.com`** (the Workspace inbox) as of 2026-06-14. Falls back to `john.c.craig24@gmail.com` (the founder inbox) only when unset. Read into a module-level const in `host-lead.ts`, and Vercel binds env vars at deployment time, so changing it **requires a redeploy** to take effect (it does *not* hot-swap on a running deployment). Never derived from form input. Added in the vending pivot ([ADR-060](DECISIONS.md#adr-060--vending-host-lead-gen-pivot)). | `.env.local` (optional) · Vercel (production — set, stored **non-sensitive** so the recipient stays verifiable via `vercel env pull`) | Low — a routing address, not a credential | `app/actions/host-lead.ts` |
| `DISCORD_BOT_TOKEN` | Discord bot token for the Foil HQ ops bot. See [ADR-013](DECISIONS.md#adr-013--foil-hq-discord-bot-persistent-memory-ops-chat-with-curated-tools). | `bot/.env.local` · Railway (production) | Secret | `bot/src/index.ts`, `bot/src/handlers/slash-commands.ts` |
| `DISCORD_APPLICATION_ID` | Numeric Discord application id (`1507171299422765116`). Used for slash-command registration. | `bot/.env.local` · Railway (production) | Public (but stored as secret for parity) | `bot/src/handlers/slash-commands.ts` |
| `DISCORD_GUILD_ID` | Optional Discord guild id — when set, slash commands register guild-scoped (instant propagation) instead of global (1-hour). | `bot/.env.local` (optional) · Railway (optional) | Public | `bot/src/handlers/slash-commands.ts` |
| `RAILWAY_PROJECT_ID` | Railway project id used by `railway link`. Local-only — Railway runtime doesn't read this. | `bot/.env.local` (deploy only) | Public | `bot/` deploy workflow (manual) |
| `OPENAI_API_KEY` | OpenAI key for `text-embedding-3-small` (1536-dim) — used by the Discord bot for real semantic recall (see [ADR-014](DECISIONS.md#adr-014--outbound-discord-notifications-per-channel-webhooks-soft-fail-single-import-boundary)). | `.env.local` · Vercel (production + preview + development) · GitHub Actions · Railway (foil-bot) | Secret | `bot/src/embed.ts`, `bot/scripts/backfill-embeddings.ts` |
| `DISCORD_WEBHOOK_DEPLOYS` | Discord webhook URL for `#deploys`. Wired via Vercel's native Discord integration (Marketplace install — no code path uses it directly today). | `.env.local` (record only) | Secret | Vercel Discord integration |
| `DISCORD_WEBHOOK_CONTENT_ENGINE` | Discord webhook URL for `#content-engine`. Fires from `scripts/generate-weekly-post.ts` after every successful blog + newsletter publish. | `.env.local` · GitHub Actions (autonomy workflow) | Secret | `lib/notifications/discord.ts::postContentPublished` |
| `DISCORD_WEBHOOK_SUBSCRIBERS` | Discord webhook URL for `#subscribers`. Fires from `app/actions/subscribe.ts` on every successful Beehiiv subscribe. Emails are masked before posting. | `.env.local` · Vercel (production + preview + development) | Secret | `lib/notifications/discord.ts::postSubscriberJoined` |
| `DISCORD_WEBHOOK_ERRORS` | Discord webhook URL for `#errors`. Fires from any soft-fail path (content engine gate exhaustion, subscribe Beehiiv failure) + the workflow's `if: failure()` step. | `.env.local` · Vercel (production + preview + development) · GitHub Actions | Secret | `lib/notifications/discord.ts::postError`, `.github/workflows/weekly-content.yml` |
| `VERCEL_WEBHOOK_SECRET` | HMAC-SHA1 signing secret for the Vercel deploys webhook proxy (see [ADR-016](DECISIONS.md#adr-016--vercel-deploys--discord-via-code-controlled-webhook-proxy-not-marketplace-install)). Generated by `vercel webhooks create`; rotate by deleting + re-creating the webhook. | `.env.local` · Vercel (production + preview + development) · GitHub Actions | Secret | `app/api/webhooks/vercel-deploys/route.ts` |
| `DIGEST_MODE` | Routing switch for outbound Discord notifications. `"daily"` queues events to `digest_events` for a once-daily batch (see [ADR-018](DECISIONS.md#adr-018--daily-digest-queue-opt-in-noise-control-via-digest_mode)); anything else (including unset) → real-time per-event pings. | Vercel (production + preview + development) — flip without redeploy | Public (config-level) | `app/actions/subscribe.ts`, future event producers |
| `SUPABASE_ACCESS_TOKEN` | Long-lived Supabase personal access token for headless CLI access (see [ADR-009 amendment](DECISIONS.md#adr-009--local-cli-tooling-for-autonomous-infra-changes)). Read automatically by `supabase` CLI when set. Rotate by revoking at supabase.com/dashboard/account/tokens. | `.env.local` · GitHub Actions · Railway (`foil-bot` service) | Secret | `supabase` CLI invocations from any goal |
| `RAILWAY_API_TOKEN` | Long-lived Railway account API token for headless CLI access. **Use `RAILWAY_API_TOKEN`, not `RAILWAY_TOKEN`** — the latter is reserved for project-scoped tokens and rejects account tokens. Rotate by revoking at railway.app/account/tokens. | `.env.local` · GitHub Actions | Secret | `railway` CLI invocations from any goal |
| `RAILWAY_TOKEN` | Alias of `RAILWAY_API_TOKEN` (same value). Stored under this name to satisfy the Session-14 goal criterion which named `RAILWAY_TOKEN` literally. **Does NOT authenticate the CLI when used directly** — Railway's CLI rejects account tokens passed via this env var with "Invalid RAILWAY_TOKEN". Use `RAILWAY_API_TOKEN` for actual invocations. | `.env.local` · GitHub Actions | Secret | Documentation only — do not invoke `railway` with this var |
| `EBAY_CAMPAIGN_ID` | eBay Partner Network campaign id (`5339154326`). Stamped into every affiliate URL via `lib/affiliate/epn.ts::buildAffiliateUrl`. Without it the page still renders but affiliate attribution drops (soft-fail). See [ADR-021](DECISIONS.md#adr-021--epn-as-v1-live-listing-source-browse-api-deferred). | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/affiliate/epn.ts` |
| `EBAY_EPN_ACCOUNT_SID` | EPN Account SID — embedded in the path of EPN API calls (`api.partner.ebay.com/v1/{SID}/products`). | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/affiliate/epn.ts::searchProducts` |
| `EBAY_EPN_AUTH_TOKEN` | EPN auth token — rides as `Authorization: Bearer <token>` on every EPN call. Rotate at partnernetwork.ebay.com → Account Settings. | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/affiliate/epn.ts::searchProducts` |
| `EBAY_DELETION_VERIFICATION_TOKEN` | Shared secret for the eBay Marketplace Account Deletion compliance webhook (see [ADR-022](DECISIONS.md#adr-022--marketplace-account-deletion-compliance-via-subscribe-path-over-exemption)). Baked into the GET challenge hash AND keys the HMAC-SHA256 verify on POST notifications. Rotation path: regenerate, update in `.env.local` + Vercel (prod + dev) + GH Actions, then resubmit the subscription form at developer.ebay.com → Alerts & Notifications → foil → Production. | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `app/api/webhooks/ebay-marketplace-deletion/route.ts`, `lib/ebay-marketplace-deletion.ts` |
| `EBAY_DEVELOPER_APP_ID` | eBay developer-account App ID for the `foil` production keyset (value: `JohnCrai-foil-PRD-4183f64d5-2a0e777e`). Paired with `EBAY_DEVELOPER_CERT_ID` as the OAuth `client_credentials` client/secret. Stored as a secret even though eBay calls it "App ID" — leaking it pairs with the Cert ID to forge access tokens. | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/affiliate/ebay-oauth.ts::getAccessToken` |
| `EBAY_DEVELOPER_CERT_ID` | eBay developer-account Cert ID for the `foil` production keyset. Acts as the OAuth client_secret half of `client_credentials` against `api.ebay.com/identity/v1/oauth2/token`. Empirically verified format ends in 4 hex chars (eBay's current shape — training-data assumption of 12 hex tail was wrong). Rotate at developer.ebay.com → My Account → Keys → "Renew Cert ID". | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/affiliate/ebay-oauth.ts::getAccessToken` |
| `CRON_SECRET` | Bearer secret guarding `/api/cron/*` routes. The wishlist alert cron (ADR-024) checks `Authorization: Bearer ${CRON_SECRET}` and 401s on mismatch. Vercel Cron Jobs read the same env var and stamp the header automatically; manual invocations from the CLI use the same shape. 64-char alphanumeric hex; rotate by generating a new value + mirroring across all three surfaces in one shot. | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `app/api/cron/wishlist-alerts/route.ts` |
| `UNSUBSCRIBE_TOKEN_SECRET` | HMAC-SHA256 key for signing one-click unsubscribe tokens (RFC 8058) embedded in every transactional / wishlist email + the visible body link (see [ADR-027](DECISIONS.md#adr-027--unified-email-capture-across-three-surfaces-default-checked-newsletter-opt-in-on-the-watchlist-form)). The token IS the identity proof for `/api/unsubscribe` — the secret can never appear in any client bundle or response body. 32+ random bytes; rotate by generating a new value + mirroring across all three surfaces (rotating invalidates unsubscribe links in already-sent emails — accept that trade only if compromised). | `.env.local` · Vercel (production + development) · GitHub Actions | Secret | `lib/unsubscribe-token.ts`, `lib/notifications/resend.ts::sendTransactionalEmail`, `lib/wishlist/scan-batch.ts`, `app/api/unsubscribe/route.ts` |
| `X_BOT_LIVE` | **Kill-switch for the daily X content bot ([ADR-058](DECISIONS.md#adr-058--daily-x-content-bot-dry-run-first-own-posts-only-satori-image-not-playwright)).** Default/unset/anything-but-`"true"` = **dry-run** (generates a draft to Discord `#content-engine`, NEVER posts to X). Set to exactly `"true"` to enable live posting once the X app + spending cap are set up (see [docs/runbooks/x-bot.md](runbooks/x-bot.md)). | Vercel (production) — flip without redeploy | Public (config-level) | `app/api/cron/x-post/route.ts`, `lib/social/bot.ts` |
| `X_API_KEY` · `X_API_SECRET` · `X_ACCESS_TOKEN` · `X_ACCESS_SECRET` | OAuth 1.0a **user-context** tokens for the Foil X account (consumer key/secret + access token/secret). User tokens are required — app-only Bearer cannot post. Absent today (bot is dry-run); John creates them at console.x.com per the runbook. The bot only reads them when `X_BOT_LIVE=true`. | `.env.local` · Vercel (production) — set at enable time | Secret | `lib/social/x-client.ts` (the only X API boundary) |
| `YT_DLP_COOKIES` | **Rotatable secret (R-018 Path A, ADR-050).** CONTENTS of a Netscape-format `cookies.txt` exported from John's logged-in YouTube browser session. Lets the daily transcript-ingestion workflow authenticate yt-dlp past YouTube's datacenter-IP bot-block (CI fetches 0 transcripts without it). Unset locally = fine (residential IP needs no cookies). **Rotation cadence: ~2-4 weeks** (YouTube cookies expire). **Procedure:** in the browser, export youtube.com cookies via the "Get cookies.txt LOCALLY" extension (or yt-dlp's `--cookies-from-browser` export) to `cookies.txt`, then `gh secret set YT_DLP_COOKIES < cookies.txt`; confirm with `gh workflow run transcript-ingestion.yml` → transcripts > 0. Re-do when CI ingestion drops back to "0 new transcripts". Optional (workflow soft-fails without it). | GitHub Actions (autonomy) · optionally `.env.local` for local CI-parity testing | Secret | `scripts/ingest-transcripts.ts`, `.github/workflows/transcript-ingestion.yml` |

---

## Pending (referenced in plans, not yet wired)

| Name | Purpose | Trigger to wire | Notes |
|------|---------|----------------|-------|
| `SCRYDEX_API_KEY` | Scrydex per-card endpoint. | [ROADMAP item #9](ROADMAP.md#next--next-2-weeks-2026-05-28--2026-06-10) — Scrydex migration when waitlist hits ~50 OR PokeTrace rate limits bite. | Needs Scrydex account + waitlist approval. |
| `TCGPLAYER_AFFILIATE_ID` | TCGplayer affiliate code appended to outbound "Buy on TCGplayer" links. | When affiliate program approval lands. | Application sent? Track in SESSION-LOG when status changes. |
| `EBAY_CAMPID` | eBay Partner Network campaign ID for outbound "Buy on eBay" links. | EPN application approval. | Same as TCGplayer. |

---

## Local dev tooling note

`.env.local` is the source of truth for local development. `vercel env pull` pulls the Vercel-stored env vars and by default writes them into `.env.local`, OVERWRITING any keys you have set locally for development. To avoid clobbering local dev keys, always pull into a separate file:

```sh
vercel env pull .env.vercel
```

Then diff manually if you want to reconcile: `diff .env.local .env.vercel`. Never run a bare `vercel env pull` in this repo.

## Where to set each scope

- **`.env.local`** — local development. Never committed. Loaded by `next dev` automatically and by scripts via the inline regex parser in each `scripts/*.ts` (see e.g. `scripts/generate-weekly-post.ts`).
- **Vercel (Production / Preview / Development)** — set via Vercel dashboard → Project → Settings → Environment Variables. The 3 environment scopes are independent; staging vs prod differences land here.
- **GitHub Actions (Repository Secrets)** — Settings → Secrets and variables → Actions → Secrets tab. Read by `${{ secrets.NAME }}` in workflow YAML.
- **GitHub Actions (Repository Variables)** — same path, Variables tab. Read by `${{ vars.NAME }}`. Use for non-sensitive config like `AUTO_PUBLISH_WEEKLY_POSTS`.
- **Supabase Edge Functions** — Supabase dashboard → Edge Functions → Secrets. Not currently used; if we add one for notifications later, log the secret here.
- **Stripe Dashboard** — webhook secrets are generated by Stripe; copy from Dashboard → Developers → Webhooks → endpoint.

---

## Sensitivity rules

- **Public** vars start with `NEXT_PUBLIC_` and are inlined into the client JS bundle by Next. Anything in this column shows up in the browser; never put secrets here.
- **Secret** vars are server-side only. Loaded from `process.env.X` in server components, API routes, server actions, and scripts. If you reference one in a client component, build will fail (or worse — silently undefined at runtime).
- When in doubt: secret. The `NEXT_PUBLIC_` opt-in keeps client-bundle leakage explicit.
