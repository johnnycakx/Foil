# Foil Roadmap

**Last updated:** 2026-06-06 (Roadmap reconciliation — folded [PLAN-2026-06-05](PLAN-2026-06-05.md) + [BUSINESS-MODEL-2026-06-05](BUSINESS-MODEL-2026-06-05.md) into the second brain. Marked **B.4 / B.6 / F4 / X1 deployed + live-verified** (commits `0381900`, `4ce1cd8`, `3c45aeb`, `93d63fc` — the /deals leaderboard, click-time deal redirect + self-hosted hero, like-for-like condition+language matching, and the dry-run X bot are all live; X1 still needs John's go-live creds). Closed the **SEO/indexability audit by observation** (GSC's 3 "page with redirect" rows are http→https/www→apex 301s working correctly; the 7 "crawled, not indexed" are `_next/static` noise; the one real defect — `/deals` missing from sitemap.xml — is fixed this session). Added the **utility-first reframe** ([ADR-059](DECISIONS.md#adr-059--utility-first-positioning--subscription-ready-not-paywalled)) + new rows: structured-matching backbone, the subscription-ready seam + instant-alerts wedge (gated), and the X-bot go-live John-manual row; clarified **B.5 as subsumed** by the recurring instant-alerts sub. Sitemap fix: `/deals` + `/pricing-methodology` added to `app/sitemap.ts` (extracted `LANDING_PATHS` → `lib/seo/sitemap-landings.ts`, pinned by `sitemap.test.ts`). Prior header below.)

**Prior update:** 2026-05-30 (Session 47.4 (cont.) — **fact-check + catalog expansion 207 → 1,007**. Corrected 4 live posts' fabrications (Moonbreon 15-20× off, 3 dead links, invented "Foil's scan data" stats); added **quality gates 9 (link existence) + 10 (Foil-data provenance)** so they can't recur; shipped **tiered per-card rendering** + a ranked-expansion pipeline and ran the first wave to 1,007 cards ([ADR-046](DECISIONS.md#adr-046--tiered-per-card-rendering--catalog-expansion-to-1000-cards), 1006/1007 PokeTrace-matched, long-tail tier skips eBay so quota stays flat). Pivoted the ranking from PokeTrace sale-volume (infeasible — no list sort) to SDK TCGplayer price. NEXT #8 marked done; second wave (→2,000) gated on indexing + Browse telemetry ([R-012](RISKS.md)). Earlier 47.4: **fixed the autonomous content-engine deploy** (ADR-045 — committer email → team-associated so Vercel stops blocking; R-011). Prior: 49c daily price-history chart (ADR-044); 49b watchlist write path (ADR-043); 49.2 PokeTrace **207/207**. Open followups: sales-volume strip + multi-condition overlay (49d); per-facet AND eBay gate; optimize the 8 above-the-fold hero PNGs; coral-error-text + `slate/60`-placeholder a11y fixes on `/start` + `/upload`.)
**Owner:** John Craig (solo)
**Cadence:** Updated at the end of every goal. See [Project Second Brain](../CLAUDE.md#project-second-brain) for the auto-maintenance contract.

The roadmap has four buckets. NOW is what's actively blocking the next ship. NEXT is the queue I'll pull from once NOW clears. LATER is committed direction but not yet scheduled. PARKED is explicitly deferred — re-check at launch + 30d.

**⚠️ Strategic context (2026-06-23 onward — DUAL-TRACK, supersedes the full-repurpose framing below).** The vending pivot's *full repurpose* is reversed ([ADR-064](DECISIONS.md#adr-064--dual-track-site-deal-finder-restored-as-primary-indexed-seo-surface-vending-lead-gen-kept-at-host)): the **Pokémon-card deal-finder is the primary public, indexed SEO surface again** (homepage at `/`, `/cards`+`[slug]`, `/deals`, `/start`, `/blog` + all posts, the pillars, the content engine reverted to card-focus + crons re-enabled), recovering the organic traffic the repurpose cut off. **ALL vending lead-gen is preserved + still indexed as a secondary track at `/host`** (`/host`, `/faq`, `/service-areas`+cities, `host_leads`, the lead form, the 3 vending blog posts, GBP assets). `AUTO_PUBLISH_WEEKLY_POSTS` is **ON** (autonomous publishing — John's intentional decision, 2026-06-25; the content engine commits twice-weekly posts to main unreviewed behind the 8 quality gates + content-marker verification. **Do NOT revert it to `false`** — the older "stays false / reviewed mode" notes in the rows below are superseded). Built 2026-06-23, pending John deploy + his restore of the PokeTrace/eBay keys (card pages soft-fail to a no-pricing render without them, never a 500). The 2026-06-13 vending-pivot block below is retained for history.

**⚠️ Strategic context (2026-06-13 onward — VENDING PIVOT, full-repurpose framing SUPERSEDED 2026-06-23 by the dual-track block above).** The public `foiltcg.com` surface was repurposed *in its entirety* from the deal-finder into a **B2B lead-gen site for a Pokémon card vending-machine placement business** (attract business owners to *host* a machine for a revenue share). The deal-finder was made **dormant** — code/data preserved in-tree, but noindexed, unlinked, off the sitemap, with all crons disabled. (Dual-track restore 2026-06-23 reverses the dormancy; the vending lead-gen build itself is fully preserved.) **Goal A built 2026-06-13** ([ADR-060](DECISIONS.md#adr-060--vending-host-lead-gen-pivot-public-surface--vending-deal-finder-dormant); new `/`, `/host`, `/faq`, `/service-areas[/city]`, Resend lead capture). Source of truth: [docs/vending/](vending/) (00–08) + Goal A in [03-CLAUDE-CODE-PROMPTS.md](vending/03-CLAUDE-CODE-PROMPTS.md). **Note:** [STRATEGY-VENDING-2026-06-12.md](STRATEGY-VENDING-2026-06-12.md) (an *additive* model — deal-finder stays + vending added) is **superseded** by the 06-13 full-replacement decision; its Jun-12 build (`/machines`, restock alerts) is preserved dormant for Phase V-2. The deal-finder strategic context below is retained for the dormant surface.

**Strategic context (2026-05-23 onward — DEAL-FINDER, public framing superseded by the vending pivot above; still accurate for the dormant surface).** Foil ships V1 as a buyer-side Pokemon TCG deal-finder — per-card landing pages, eBay-aggregated best-listing recommendation, wishlist email alerts. Scanner functionality is preserved in-tree but deferred from V1 launch scope (V2 candidate). Content engine + newsletter + autonomy stack remain intact and reframe content topics to buyer-intent shape. Source: [STRATEGY-PIVOT-DEAL-FINDER.md](STRATEGY-PIVOT-DEAL-FINDER.md), formalized in [ADR-020](DECISIONS.md#adr-020--pivot-to-buyer-side-deal-finder-positioning).

---

## NOW — this week (≤ 2026-05-27)

| # | Item | Why it's NOW | Owner | Status |
|---|------|--------------|-------|--------|
| MM | **Market-movers / "good buys" insight signal — the insight-led /deals core + the Moonbreon like-for-like fix** <!-- added 2026-06-25, ADR-069 --> | Implements [STRATEGY-DATA-INSIGHT-ENGINE.md](STRATEGY-DATA-INSIGHT-ENGINE.md). Replaces fragile single-listing deals (the £1,000 LP/UK/GBP "Moonbreon" false deal) as the LEAD signal with market-level momentum from PokeTrace's windowed aggregates (NM `(avg7d-avg30d)/avg30d`), sample-size gated. Powers the newsletter, the /deals board, and (later) X. | ✅ **BUILT 2026-06-25 ([ADR-069](DECISIONS.md#adr-069--insight-led-market-movers--good-buys-signal-aggregate-momentum-over-fragile-single-listings--the-like-for-like-currency-gate)) — pending John deploy.** Pure momentum core + injectable batch; `market_movers` (current-state) + append-only `market_snapshots` (time-series seed); new `/api/cron/market-movers` (09:00 UTC, curated tier, rate-limited); /deals leads with "Good buys this week" + demotes the single-listing board; the like-for-like **currency gate** in `computeCardBuySignal` (non-USD → UNKNOWN) + R-010 fixture/test; deterministic newsletter digest serializer + `scripts/generate-movers-digest.ts`. Gates: tsc clean · `npm test` 989/971-pass/0-fail/18-skip · build · design:lint (3 pre-existing warnings, 0 on new surfaces) · /security-review. **PokeTrace key is load-bearing (valid ~until July 15); signal soft-fails to empty without it.** **John: restore PokeTrace key → `npm run dev` review → push → after deploy the movers cron populates the board, then run `scripts/generate-movers-digest.ts` for newsletter issue #1's source.** **Follow-ups:** longtail momentum off the snapshots; week-over-week "cards on the move"; feed the X bot; recurring auto-digest. **✅ DEPLOYED + cron confirmed running 2026-06-28:** `market_movers` repopulates daily at 09:03 UTC (296 rows that day, 8 down / 11 up, PokeTrace live); the "good buys" digest for newsletter issue #1 was regenerated from this fresh cache and staged paste-ready (SESSION-LOG 2026-06-28). | Claude Code + John | ▶ Live · issue #1 staged |
| V-REV | **Dual-track restore — deal-finder = primary indexed SEO surface; vending kept at /host** <!-- added 2026-06-23, ADR-064 --> | The full vending repurpose cut off the deal-finder's compounding organic traffic. Reverse the dormancy (un-noindex card/blog/pillar routes, restore the homepage + sitemap + nav + content engine + crons) while preserving ALL vending lead-gen at `/host`. | ✅ **BUILT 2026-06-23 ([ADR-064](DECISIONS.md#adr-064--dual-track-site-deal-finder-restored-as-primary-indexed-seo-surface-vending-lead-gen-kept-at-host)) — pending John deploy.** Gates: tsc clean · `npm test` 941/923-pass/0-fail · build exit 0 · design:lint exit 0 · /security-review (see SESSION-LOG). `AUTO_PUBLISH_WEEKLY_POSTS` was `false` at build; now intentionally **ON** (autonomous, 2026-06-25 — see the strategic-context note above; do not revert). **John: restore PokeTrace/eBay keys to Vercel → `npm run dev` review → push to deploy → resubmit sitemap to GSC + request reindexing → run `CONTENT_VERIFY_BASE_URL=https://foiltcg.com npm test`.** | Claude Code + John | ▶ Built, pending deploy |
| G-EMAIL | **Homepage reorient → email capture as primary goal + inline capture on ranking pages** <!-- added 2026-06-24 --> | GSC (3-mo) shows research-intent traffic (condition/value queries) at 0.7% CTR, avg pos 18.2 — the right conversion for this audience is an email signup, not an affiliate click. The owned email list is the committed compounding moat ([STRATEGY-AUDIENCE-MOAT.md](STRATEGY-AUDIENCE-MOAT.md)). Reorient the homepage so subscription is the PRIMARY CTA (deal-finder demoted to "the reason to subscribe"); add tasteful inline `EmailCapture` to the blog/pillar pages (where impressions actually land); GSC queries become the content+newsletter calendar. Builds on the V-REV dual-track restore. ✅ **BUILT 2026-06-24 ([ADR-065](DECISIONS.md#adr-065--homepage-reorient-email-capture-is-the-primary-conversion-goal-inline-capture-on-the-ranking-content-surfaces)) — pending John deploy.** Hero leads with the newsletter (`source=homepage_hero`) + demoted deal links; blog/pillar capture refined (`blog_inline` + `pillar_*` tags). Premise check caught the 3 pillars stuck on the pre-cream dark palette (white/zinc text on cream = live AA contrast bug) — John approved the **full re-migration** to cream/navy/gold + added them to `PUBLIC_SURFACES`. Gates: tsc clean · `npm test` 946/928-pass/0-fail · build exit 0 · design:lint exit 0 (3 pre-existing warnings, none on changed files) · /security-review. **John: `npm run dev` review → push → after deploy run `CONTENT_VERIFY_BASE_URL=https://foiltcg.com npm test` + map GSC research-intent queries into the content/newsletter calendar.** ➕ **Homepage v2 refinement 2026-06-24 (same ADR-065):** distilled the hero to ONE value prop, dropped the insider "Level-4 TCGplayer seller" jargon (all 3 homepage spots + meta), and added a founder presence (headshot + plain byline) as the trust signal + X-pipeline brand seed; deal links stay free/indexable + demoted. Gates green (948/930/0). | Claude Code + John | ▶ Built, pending deploy |
| G-MAGNET-2 | **Overpay lead magnet — "The Pokémon Cards People Overpay For Most"** <!-- added 2026-06-24, ADR-068 --> | The data-backed lead magnet. [ADR-068](DECISIONS.md#adr-068--foils-first-lead-magnet-the-evergreen-pricing-cheat-sheet-gated-on-page-data-availability-driven-choice) shipped the **evergreen cheat-sheet fallback** because the overpay data wasn't clean: PokeTrace was cancelled 2026-06-16 and `buy_signals` was 11 days stale with only 5 usable ABOVE rows. **Unblocked once John restores PokeTrace + the `deals-refresh` cron repopulates `buy_signals` with fresh ABOVE rows** — then build "cards whose live ask sits above recent sold," a Foil-unique live-data magnet tied to the "stop overpaying" hero. Reuse the `/free/` + `LeadMagnetGate` pattern; honest figures, soft-fail (show fewer entries if thin). | Claude Code + John | Pending (blocked on PokeTrace restore) |
| V-A | **Vending pivot — Goal A (public surface → vending host lead-gen)** <!-- added 2026-06-13, ADR-060 --> | The whole public product changed direction. New `/`, `/host`, `/faq`, `/service-areas[/city]` (8 Tier-1 cities); Resend lead capture to john.c.craig24@gmail.com; deal-finder dormant (noindex + unlinked + sitemap rebuilt + crons disabled). | ✅ **BUILT 2026-06-13 ([ADR-060](DECISIONS.md#adr-060--vending-host-lead-gen-pivot-public-surface--vending-deal-finder-dormant)) — pending John deploy.** Gates: tsc clean, `npm test` 932/0-fail, build + design:lint + /security-review (see SESSION-LOG). Premise check surfaced + reconciled the 06-12 additive vs 06-13 replace conflict (held, proceeded, flagged in ADR-060). **John: deploy + resubmit the new sitemap to GSC.** | Claude Code + John | ▶ Built, pending deploy |
| V-B | **Vending pivot — Goal B (proof + finalize placeholders)** <!-- added 2026-06-13; polish pass done 2026-06-13 --> | The site ships with visible `[PLACEHOLDER]`s (operator liability insurance on /faq) + needed proof assets. | ✅ **Polish pass BUILT 2026-06-13 (pending John deploy):** integrated John's 2 clean tower photos as **neutral product/model imagery** (homepage hero + /host) — per John's mid-task correction these are partner-network MODEL reference shots, NOT Foil installs, so no placement claims/venue names; dropped the 2 wall shots that surface "MobysArcade". Impeccable critique→polish: detector clean on all vending surfaces, **em dashes removed from all user-facing copy**, placement-ambiguous homepage line fixed. **Corrective pass 2026-06-13 (John OFF-SITE decisions):** insurance/liability removed from the public site entirely (call topic) and the revenue-share % + "gross" removed from all copy (also a call topic) — both are now resolved by removal, no longer pending placeholders; the site renders no insurance/liability claim and no placeholder text. **Still needs John:** base city/address (GBP verification), honest install timeline, public phone, a real testimonial. Then a final copy + OG-image pass. | John + Claude Code | ▶ Copy corrections done; remaining inputs pending John |
| V-E | **Vending pivot — Goal E (homepage redesign + design-system evolution)** <!-- added 2026-06-13, ADR-061 --> | Live review: homepage copy overwhelming, the site lacks contrast / feels bleak, the hero photo floats on cream. Root cause: the surfaces inherited the deal-finder's quiet-backroom register. | ✅ **BUILT 2026-06-13 ([ADR-061](DECISIONS.md#adr-061--vending-register-evolve-the-quiet-backroom-canon-for-the-b2b-host-audience)) — pending John deploy.** Evolved the canon for the vending audience ("confident local operator": cream↔navy alternation, subtle resting elevation, gold as a structural accent; palette/fonts/anti-references kept) in DESIGN.md §7 + PRODUCT.md. Redesigned the homepage: copy trimmed ~40% (value props 6→4, FAQ teaser cut → link to /faq, compact homepage form), a dark-navy "How it works" band with gold step numbers, resting elevation on cards, the hero photo moved onto a navy device frame, the "no testimonials/locations" lines removed from / + /host. Regenerated OG/Twitter for the vending brand. Applied the register to /host, /faq, and city pages. Gates: tsc, npm test, build, design:lint, /security-review all green. | Claude Code + John | ▶ Built, pending deploy |
| V-GBP | **Google Business Profile (Service-Area Business) setup** <!-- added 2026-06-13, doc 04 §2 --> | The single highest-ROI local-SEO lever for an SAB — and a code-independent John task. Needs the base city/address (hidden) + Bay-Area service areas; mirrors the site NAP + FAQ. | **John (manual):** create + verify GBP, define ≤20 service areas (or counties), self-populate Q&A from the site FAQ, add real machine photos, start collecting host reviews. See doc 04 §2. | John | Pending |
| V-C | **Vending pivot — Goal C (content engine reframe → vending/local SEO)** <!-- added 2026-06-13; BUILT 2026-06-15, ADR-062 --> | The content channel still targeted deal-finder/collector topics + gates, compounding search momentum toward the dormant product. | ✅ **BUILT 2026-06-15 ([ADR-062](DECISIONS.md#adr-062--content-engine-reframe-deal-findercollector--vending-host-acquisition--local-seo)) — pending John review.** Rewrote `lib/seo/content-engine.ts` SYSTEM_PROMPT for the Bay-Area host owner in the confident-local-operator voice with the honesty guardrails baked in; replaced the 3 collector pillars in `docs/seo-strategy.md` with 2 vending pillars (`/host` host-acquisition + `/service-areas` local SEO, 20 cluster candidates); refactored the gates — retired the dollar-figure-count, Foil-scan-data citation + provenance, and `/cards`-resolver gates; added V-benefit / V-geo / V-link / V-honesty; kept word-count/banned-phrase/schema/FAQ/em-dash/anti-hype/attribution. Regenerated **3 posts to `_pending/`** with the before/after measure (SESSION-LOG 2026-06-15). **PUBLISHED 2026-06-15 ([ADR-063](DECISIONS.md#adr-063--selective-index-vending-blog-surface-pillar-gated)):** the 3 reviewed drafts moved into the live `POSTS_DIR`; `/blog` re-enabled for **vending only** (pillar-gated `host`/`service-areas` indexable; 7 deal-finder posts stay noindexed/unlisted), added to nav/footer + sitemap; dormant newsletter funnels swapped for `/host` CTAs on vending posts; gas-station "25 to 45"→"25 to 40" fix. Internal links verified. Gates: tsc, `npm test`, build, design:lint, /security-review all green. **At build (2026-06-15): `AUTO_PUBLISH_WEEKLY_POSTS=false` + Mon/Thu cron commented out.** (Superseded — autonomy is now intentionally **ON** as of 2026-06-25; the cron + repo var are live. See the strategic-context note at the top.) **John: `npm run dev` review → push to deploy → run `CONTENT_VERIFY_BASE_URL=https://foiltcg.com npm test` + resubmit sitemap to GSC.** | Claude Code + John | ▶ Built + published, pending John deploy |
| V-D | **Vending pivot — Goal D (scout/referral page, optional)** <!-- added 2026-06-13 --> | Optional `/refer` funnel for the commission-based scout/connector program. | Goal D: `/refer` reusing the Goal A Resend lead pattern (separate subject tag), `/refer` added to PUBLIC_ROUTES + proxy test. Comp framing per John (no exact $ unless confirmed). | Claude Code | Pending (optional) |
| 1 | **GitHub Actions secrets:** set `ANTHROPIC_API_KEY`, `BRAVE_SEARCH_API_KEY`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` | The Monday + Thursday autonomous workflow can't run without these. First scheduled fire is Mon 2026-05-25 14:03 UTC. | John (manual GitHub settings) | Pending |
| 2 | **Homepage deal-finder hero** <!-- reframed via pivot 2026-05-23; Done 2026-05-28, Sessions 39/43/45/46 --> | Superseded the v0.dev-paste plan: the hero was rebuilt in-house across Session 39 (cream/navy/gold), 43 (grail backdrop), 45 (a11y/focus), and 46 (warmth pass — Fraunces, spark mark, pricing removed, asymmetric showcase). Launch-complete. See [ADR-036](DECISIONS.md#adr-036--home-page-warmth-pass-fraunces-display-spark-mark-pricing-removal-lighter-scrim). | Claude Code | ✅ Done 2026-05-28 |
| 3 | **Google Search Console:** add `foiltcg.com` property, TXT-verify, submit `/sitemap.xml` | Indexing latency is 1-4 weeks. The earlier we submit, the earlier the three pillars + blog + (soon) per-card landing pages start appearing in search. | John (Vercel DNS + GSC UI) | Pending |
| 4 | **Decision: keep or kill the 2 auto-generated posts** | `how-to-read-a-japanese-pokemon-card` + `near-mint-vs-lightly-played-…` shipped via the new autonomy pipeline on 2026-05-20. Both passed gates on first attempt. Read them in the live preview and decide: leave up, edit, or delete. Doubles as the calibration corpus for the founder-voice work coming in Session 22-23. | John (manual review) | Pending |
| 5 | **Per-card landing page MVP at `/cards/[slug]`** <!-- promoted via pivot 2026-05-23 --> | First concrete proof of the deal-finder direction. Charizard Base Set is the launch card — highest-value/highest-recognition single SKU, exercises every part of the stack (image, eBay listing, condition picker, schema.org Product, wishlist form). One real page beats 200 templated ones for de-risking the design. | Claude Code (next goal) | Pending |
| 6 | **eBay Browse API integration in `lib/affiliate/ebay-api.ts`** <!-- promoted via pivot 2026-05-23 --> | The data dependency for #5. Need: search by card name + set filter, parse listing nodes (price/condition/seller/shipping), wrap output URL with affiliate Campaign ID. Soft-fail per the lib/ pattern — eBay outage shouldn't 500 a landing page. Open question per strategy doc: actual quota at 500 cards × hourly refresh. Start with 1-card hourly to characterize. | Claude Code (next goal) | Pending |
| 7 | **Watchlist table schema in Supabase** <!-- promoted via pivot 2026-05-23 --> | The data dependency for the wishlist alerts cron in NEXT. Schema: `watchlists(id, email, card_slug, target_price_cents, created_at, last_notified_at)`. Email-anchored (no auth in V1 per ADR-020). Migration in `supabase/migrations/`. | Claude Code (next goal) | Pending |
| 8 | **eBay Browse API access — keyset compliance + OAuth client + page swap** <!-- Done 2026-05-24, Session 26 --> | Closed via [Session 25](SESSION-LOG.md) (Marketplace Account Deletion webhook → keyset compliant per [ADR-022](DECISIONS.md#adr-022--marketplace-account-deletion-compliance-via-subscribe-path-over-exemption)) + [Session 26](SESSION-LOG.md) (Browse API client + OAuth helper + per-card page swap per [ADR-023](DECISIONS.md#adr-023--browse-api-client-ships-libaffiliatelinksts-multi-source-selector-deferred-until-tcgplayer-access-lands)). `getBestListing()` now returns real Browse API listings end-to-end. | John (compliance form) + Claude Code (code swap) | ✅ Done 2026-05-24 |
| 9 | **Resend domain verification + `DEFAULT_SENDER` flip** <!-- Done 2026-05-24, Session 30 --> | Closed via [Session 30](SESSION-LOG.md). Resend records for `foiltcg.com` verified in the Vercel-managed DNS between Session 28's 403 test (~21:11 UTC) and the natural 22:00 UTC hourly cron tick on 2026-05-24 — 6 production wishlist alert emails delivered end-to-end pre-flip from the test-mode `onboarding@resend.dev` sender. This goal flipped `DEFAULT_SENDER` + the inline `sendTransactionalEmail` fallback to `Foil <alerts@foiltcg.com>`; structural regression-guard test pinned `onboarding@resend.dev` cannot reappear in `lib/` or `app/` outside the test file. | John (DNS + Resend dashboard) + Claude Code (sender flip) | ✅ Done 2026-05-24 |
| 26 | **Task #26 — MDX component palette migration (Heads up callout contrast bug)** <!-- Done 2026-05-26, Session 42 --> | Session 39's visual-identity sweep migrated every page-level surface to cream/navy/gold but missed `mdx-components.tsx`. The custom components rendered INSIDE blog-post bodies (Callout / FAQ / CardScannerEmbed / TopicLink / pre / code overrides) still shipped pre-cream tokens — the "Heads up" warning Callout rendered washed-out amber-on-cream and was effectively invisible. Session 42 migrated all 5 components to foil-* tokens, brought the MDX `pre`/`code` overrides into parity with the `prose-*` cream chain, and extended `visual-regression.test.ts` with both (a) `mdx-components.tsx` added to `PUBLIC_SURFACES` so the no-coral-default / no-raw-hex invariants now apply, and (b) 5 new component-specific structural assertions. The meta-lesson — page-level visual-regression doesn't catch component-level drift — is documented as the rationale for [ADR-031](DECISIONS.md#adr-031--mdx-component-palette-discipline-tokens-only-contrast-tested-at-the-component-layer). 518/518 tests; tsc clean; compliance:check 6/6 PASS. Twitter pinned-post launch unblocked. | Claude Code | ✅ Done 2026-05-26 |
| 24 | **Task #24 — Per-card page reference-data layer (PokeScope-style)** <!-- Done 2026-05-26, Session 41 --> | `/cards/[slug]` was a competent buyer's-action page through Session 40 but lacked the reference-data layer collectors expect when cross-referencing a card. Session 41 added 5 new components — `<Breadcrumb>` (with BreadcrumbList JSON-LD), `<CardMetadataBlock>` (Type/Series/Artist/Release year/Attacks/Weaknesses), `<LiveTimestamp>` (Client, ticks every 10s with aria-live), `<PriceRangeBar>` (Low/Mid/High track with current-listing marker), `<CardVariantsSection>` (one PriceRangeBar per TCGplayer variant slug, "Highest value" badge on the top-market variant). SDK `CardMetadata` extended with `series`, `types`, `subtypes`, `hp`, `artist`, `attacks`, `weaknesses`, `tcgplayerPrices`, `tcgplayerUpdatedAt` — all with empty defaults so soft-fail records and pre-Session-41 baked entries still render. Page composition order: Breadcrumb → Hero → Variants → LiveTimestamp → Best listing → Watchlist → CardMetadata → About → Related. The reference layer is purely additive; the buyer's-action layer is unchanged. 511/511 tests; tsc clean; compliance:check 6/6 PASS. See [ADR-030](DECISIONS.md#adr-030--per-card-page-reference-data-layer-pokescope-inspired-additions-atop-the-existing-buyers-action-layer). Four followups (graded prices, TCGplayer affiliate row, Cardmarket integration, periodic bake refresh) tracked in the ADR. | Claude Code | ✅ Done 2026-05-26 |
| 23 | **Task #23 — Pre-launch five-bug fix pass** <!-- Done 2026-05-26, Session 40 --> | Live-verification on the Session 39 deploy surfaced 5 launch-blocking issues. (1) /start typeahead empty — upstream Pokemon TCG SDK returning intermittent 504s; fix added retry-on-5xx (200ms + 600ms backoff, max 3 attempts) to all 4 SDK entry points in `lib/cards/sdk.ts`. (2) Blog YAML frontmatter rendering as paragraph text — added `remark-frontmatter` v5.0.0 to `next.config.ts` `createMDX` plugin chain (the metadata path via `gray-matter` already worked; the MDX body compiler was missing the plugin). (3) /cards/sets/[id] rendering 12/16 cards as gray placeholders — same SDK 504 root cause as Bug 1, same retry-on-5xx fix. (4) Homepage hero card grid lacked depth against cream — added `shadow-xl shadow-foil-navy/30`, dropped translucent opacity, added soft scrim gradient behind the card row only. (5) /start showed redundant `FooterEmailCapture` widget below the in-form opt-in — `components/footer-email-capture.tsx` now uses `usePathname()` + `SUPPRESS_ON_ROUTES = ["/start"]` to return null on /start. New `lib/__tests__/cards-search-route.test.ts` pins the `{hits:[...]}` wire contract + retry-recovery behavior (the form's typeahead is a wire-contract failure mode we've burned a launch on once already). 495/495 tests; tsc clean; compliance:check 6/6 PASS. Twitter pinned-post launch unblocked again. | Claude Code | ✅ Done 2026-05-26 |
| 22 | **Task #22 — Visual identity overhaul (cream + navy + gold)** <!-- Done 2026-05-26, Session 39 --> | Day-after-Session-38 founder design call concluded the dark/coral palette still read as "indie SaaS template." Session 39 retunes the Aceternity primitives + every public surface onto a cream/navy/gold collector-niche palette: `--color-foil-cream` (#F8F5F0) page BG, `--color-foil-navy` (#0F1E3A) text + CTA bg, `--color-foil-slate` muted text, `--color-foil-gold` (#C9A24B) premium/foil/live accent, `--color-foil-coral` (#FF6B5C) demoted to **hover-state only**. `BackgroundGradientAnimation` gains a `variant` prop — new default `"corner-shimmer"` replaces the full-page rainbow with a restrained bottom-right gold/navy glint. `Card3D` + `MagneticButton`/`MagneticLink` ship default gold hover-ring + shadow-expansion. `Sparkles` removed from hero (component stays exported for any future surface). `/start` form drops `1./2./3.` step numbering (the conditional step-2 created a visible 1→3 jump for first-time visitors) in favor of named section headers. New `visual-regression.test.ts` pins the no-coral-default rule + no-raw-hex rule across 15 public-surface files. See [ADR-029](DECISIONS.md#adr-029--cream--navy--gold-visual-identity-for-collector-niche-distinctiveness). Three followups (`prefers-reduced-motion`, per-card Card3D wrap, Cabinet Grotesk via `next/font/local`) tracked in the ADR. Twitter pinned-post launch unblocked. | Claude Code | ✅ Done 2026-05-26 |
| 13 | **Task #20 — /start multi-card onboarding + Aceternity-UI aesthetic refresh** <!-- Done 2026-05-25, Session 38 --> | The headline Twitter-CTA target ([STRATEGY-AUDIENCE-MOAT.md](STRATEGY-AUDIENCE-MOAT.md)) is now a single page where a visitor types a few card names, each becomes a chip with an optional price target, picks newsletter opt-in, and submits — N watchlist rows + Beehiiv subscriber in one shot. Replaces the per-card friction of "search → land on page → fill form" with multi-add. Bundled with a niche-distinctive aesthetic refresh — Aceternity-pattern components (`BackgroundGradientAnimation`, `Card3D`, `MagneticButton`/`MagneticLink`, `Sparkles`) shipped code-owned MIT in `components/aceternity/`, pure-CSS rewrites of the framer-motion originals so the bundle stays light. Brand-tuned holographic palette (`#FF6B5C` + teal + violet + amber) + Bricolage Grotesque display font substitution (Cabinet Grotesk isn't on Google Fonts) drift-guarded in CI. Lucene-injection guard added to `searchCards` (the SDK uses Lucene; raw user input would otherwise pivot the query). 467/467 tests; tsc clean; compliance:check 6/6 PASS. Twitter post unblocked. See [ADR-028](DECISIONS.md#adr-028--aceternity-ui-patterns-code-owned-no-npm-vendor-niche-visual-identity). Four followups (Card3D /cards wrap, prefers-reduced-motion, set-name Text Hover Effect, Cabinet Grotesk via next/font/local) tracked in the ADR. | Claude Code | ✅ Done 2026-05-25 |
| 12 | **Task #18 — unified email capture + Privacy/ToS + RFC 8058 unsubscribe** <!-- Done 2026-05-25, Session 37 --> | The audience-moat infrastructure per [STRATEGY-AUDIENCE-MOAT.md](STRATEGY-AUDIENCE-MOAT.md). Folds in the former standalone Privacy/ToS task (#9 in earlier planning, now absorbed). Three email-capture surfaces (watchlist form opt-in checkbox + `/newsletter` landing + footer email capture) all write to the same Beehiiv list with distinct source tags. Plain-language Privacy + ToS at `/legal/privacy` + `/legal/terms` from a shared content module. HMAC-signed unsubscribe tokens at `lib/unsubscribe-token.ts`; `/api/unsubscribe` route handles both GET (visible link) and POST (RFC 8058 List-Unsubscribe-Post) — Gmail / Yahoo / Apple Mail now render the inbox-level unsubscribe button. New env var `UNSUBSCRIBE_TOKEN_SECRET` mirrored across all three surfaces. DMARC TXT record (`_dmarc.foiltcg.com = "v=DMARC1; p=none;"`) is a founder-manual step flagged in SESSION-LOG. See [ADR-027](DECISIONS.md#adr-027--unified-email-capture-across-three-surfaces-default-checked-newsletter-opt-in-on-the-watchlist-form). Four followups (lifecycle automation, sender reputation, engagement metrics, EU GDPR consent path) tracked as Task #19. | Claude Code (code) + John (DMARC DNS) | ✅ Done 2026-05-25 |
| 11 | **Task #17 — quality-aware listing picker** <!-- Done 2026-05-25, Session 36 --> | Highest-leverage product bug fix on the build. Wishlist-alert email on 2026-05-25 surfaced a $1.75 keyword-stuffed Venusaur listing as "NEAR MINT" when real market is $40-80; same failure mode poisoned every `/cards/[slug]` page render and would block the catalog-expansion sprints in [STRATEGY-PROGRAMMATIC-SEO.md](STRATEGY-PROGRAMMATIC-SEO.md). Fix replaces lowest-absolute-price-wins in `lib/affiliate/ebay-browse.ts::getBestListing` with a pure 4-stage quality picker in `lib/affiliate/listing-picker.ts` — outlier rejection + title-junk keywords + condition keywords + lowest-price among survivors. R-010 application: 5 production-anchored fixtures in `lib/__fixtures__/ebay-listings/` (each `_observed` field cites the case). See [ADR-026](DECISIONS.md#adr-026--quality-aware-listing-picker-replaces-lowest-price-wins). Four followups (picker-decision telemetry, threshold tuning, seller-rating filter, multi-factor scoring) scoped out and tracked in the ADR. | Claude Code | ✅ Done 2026-05-25 |
| 10 | **14-day eBay Application Growth Check evidence push** <!-- added 2026-05-24 --> | Per the Application Growth Check prerequisites (`developer.ebay.com/my/support/tickets?tab=app-check`), eBay requires "application is live and approaching the default 5000/day call limit" before they'll approve a ceiling lift. We just went live with Browse on 2026-05-24 with ~100 calls/day usage. The 14-day window (submission target ~2026-06-07) accumulates real production data + builds the supporting compliance artifacts (`docs/EBAY-COMPLIANCE.md`, `/legal/ebay-api-compliance` public page, privacy/ToS update, architecture one-pager PDF) so the application is bulletproof at submission. Phase 1 (telemetry) closed Session 28. **Phase 1 evidence amplified Session 31 — 19 watchlist rows across 19 unique slugs, cron volume distributed across 24h via staggered `last_notified_at`.** Phase 2 (`docs/EBAY-COMPLIANCE.md` + invariants) closed Session 32. Phase 3 (`/legal/ebay-api-compliance` public page) closed Session 33. **Phase 4 (PDF one-pager artifact at `public/compliance/foil-ebay-api-compliance.pdf`, served from `https://foiltcg.com/compliance/foil-ebay-api-compliance.pdf`) closed Session 35.** **Phase 5 (Privacy + ToS at `/legal/privacy` + `/legal/terms`) closed Session 37 as part of Task #18 (row 12 above).** Phase 6 (eBay portal submission) remains. | John (manual seeding + Resend + submission) + Claude Code (Phase 2 goals sequentially) | In progress |
| X-bot go-live | **Flip the X content bot from dry-run to live (own-account posting)** <!-- added 2026-06-06, reconciliation; John-manual, gates X1 --> | The X bot (X1) is deployed + dry-run-verified (posts nothing); going live is the top traffic lever and is purely John-manual now. | **John (manual):** create the X app + OAuth 1.0a **user** tokens, set `X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_SECRET` in Vercel, set a console **spending cap** (~$0.20/post-with-URL; R-019), review the daily Discord `#content-engine` drafts, smoke-test the poster once, then set `X_BOT_LIVE=true`. Runbook: docs/runbooks/x-bot.md. | John | Pending |
| SEO audit | **SEO / indexability audit — CLOSED by observation 2026-06-06** <!-- queued goal closed by the GSC review --> | The queued "confirm card/catalog/deals pages return 200 to Googlebot; diagnose the 3 GSC 'page with redirect' failures" goal. | ✅ **CLOSED by observation (2026-06-06 GSC review).** The 3 "page with redirect" rows are `http://foiltcg.com`, `http://www.foiltcg.com`, `https://www.foiltcg.com` correctly **301ing to the canonical `https://foiltcg.com`** — working as intended, not a bug. The 7 "crawled, not indexed" pages are `_next/static` assets (noise). The one real defect — **`/deals` missing from sitemap.xml** — is fixed this session (also added `/pricing-methodology`). Live sitemap carries 1,023 URLs; Google last read it 2026-05-22 (209 URLs, pre-expansion); John resubmitted 2026-06-06 + requested indexing on 10 priority URLs. Residual: indexing latency / new-site authority (a distribution problem, addressed by the X bot + catalog scale + newsletter), not a settings bug. | Claude Code + John | ✅ Closed 2026-06-06 |
| NAPA-HEDGE | **Content-marker gate failure on the live `pokemon-card-vending-machine-placement-in-napa` post** <!-- added 2026-06-28, surfaced by the SEO/OG post-deploy verification; FIXED same day --> | The prod-targeted `CONTENT_VERIFY_BASE_URL` run (ADR-049 content-marker) flagged `app/(site)/blog/posts/pokemon-card-vending-machine-placement-in-napa.mdx:49` — *"At approximately $4 a month…"* — against the banned `approximately\s*\$` vague-hedge regex. **Pre-existing + unrelated to the SEO/OG push** (that push touched no vending post); invisible until then because the local pre-push gate runs without a base URL, so the content-marker network checks skip. ✅ **FIXED 2026-06-28 (commit `69286ae`, John approved):** dropped "approximately" (→ "At $4 a month…", strictly more precise). Gates: tsc clean · `npm test` 1118 (1100-pass/0-fail/18-skip) · prod content-marker re-run now **11/11**. | Claude Code + John | ✅ Done 2026-06-28 |

---

## NEXT — next 2 weeks (2026-05-28 → 2026-06-10)

| # | Item | Trigger | Notes |
|---|------|---------|-------|
| NL-SEND | **Newsletter `/approve`→send loop (parity with the X bot)** <!-- added 2026-06-28; John directive --> | The newsletter has BOTH generation engines (blog→draft + movers→digest) but NO send/approval last mile, so drafts die unsent in `docs/newsletter-drafts/`. Issue #1 was staged + manually sent 2026-06-28 (the maiden run). | Build: cadence-generate the movers digest → md→HTML → post to Discord `#content-engine` as an `/approve` card (mirror the X-bot approval flow) → on `/approve`, create+send via the Beehiiv boundary (`lib/beehiiv.ts` / `lib/beehiiv-posts.ts`). ✅ **BUILT 2026-06-28 ([ADR-077](DECISIONS.md#adr-077--newsletter-approvedeliver-loop-the-no-spend-rail-beehiiv-rss-to-send-is-maxenterprise-not-scale)) — pending John env setup + deploy.** P0 refuted the RSS-to-Send premise: per Beehiiv's official docs (updated 2026-06-04) **RSS-to-Send is Max/Enterprise, NOT Scale** — so John chose the **no-spend `/approve` rail** (defer paid auto-send). Weekly cron (`/api/cron/newsletter-digest`, Wed 14:13 UTC, `NEWSLETTER_DIGEST_MODE` default off) → quality-gate → Discord `/approve` card → on approve, email the founder the paste-ready issue (cloned the ADR-071 X rail; the bot's `/approve <id>` falls through to `/api/newsletter/approve`). Affiliate-link integrity is now a structural gate (issue-#1 bug). Gates: tsc clean · 5 new test files · npm test green · /security-review. **John (one-time):** set `NEWSLETTER_DIGEST_MODE=approval` + `NEWSLETTER_APPROVE_SECRET` (Vercel prod + the secret on the bot's Railway), apply the migration, push. ➡️ **SUPERSEDED for the send mechanism by [ADR-078](DECISIONS.md#adr-078--own-the-newsletter-send-via-resend-broadcasts-fully-automated-no-beehiiv-upgrade-no-manual-paste) (2026-06-28):** John's hard constraint = fully-automated send, no manual paste, no Beehiiv upgrade. **Built + LIVE-TESTED the Resend Broadcasts send** — `/approve` now SENDS the digest as a Resend Broadcast to the audience (we own the send; Beehiiv kept as signup/archive). HARD test gate PASSED: 6 real broadcasts delivered to John's Primary inbox, full digest renders, affiliate links EPN-tagged, native one-click unsubscribe verified (204 → contact unsubscribed). **John (one-time, additional):** apply the `newsletter_subscribers` migration; set `RESEND_AUDIENCE_ID=bb404620-b3a0-47b0-843a-f92281cf3695` (the General audience, John already backfilled) on Vercel prod; push. Recommended later: `news.foiltcg.com` sending subdomain (DNS). ➕ **Branded 2026-06-28 ([ADR-079](DECISIONS.md#adr-079--branded-newsletter-email-react-email-text-forward-to-hold-gmail-primary)):** the email is now a branded react-email template (`emails/movers-digest-email.tsx`, cream/navy/gold, text-forward, no images) — live-verified to render correctly AND stay in Gmail **Primary** (4/4, 0 Promotions). | Claude Code + John | ✅ Built + live-tested 2026-06-28 (pending John env + deploy) |
| SM | **Verified-listing resolver — the FoilTCG backbone (Set + Number + Finish + Language + condition, ONE gate every consumer shares)** <!-- added 2026-06-06; reframed 2026-06-06 from "structured-matching extension" to the unified resolver after the Japanese-Typhlosion architectural finding --> | The accuracy moat + the architectural fix John flagged after the English Typhlosion page (`neo1-17`) surfaced + redirected to a **Japanese** listing (eBay `117223259644`). Root cause: the listing PICKER (what users see/click — title-only) and the buy-signal CLASSIFIER (aspect/language gate) are separate code paths; ADR-057's gate protects only the classifier. **No more layered patches** (026/053/057 each hardened one layer). | 📐 **DESIGN APPROVED-PENDING — see [docs/DESIGN-VERIFIED-LISTING-RESOLVER.md](DESIGN-VERIFIED-LISTING-RESOLVER.md) (2026-06-06).** One `resolveVerifiedListing(cardId, condition)` → identity-verified listing or honest `null` (null beats unverified-cheapest, always); title parsing PRE-FILTERS, never solely admits. Demand-driven: resolves at page-visit for ANY catalog card (live-listing tier distinction RETIRED — **quota spends on visitors, not on pre-scanning a fixed set**); `/deals` becomes a budgeted-rotation aggregation of the same verified output. Migrates all 4 unverified consumers (per-card page, `/go` redirect, wishlist cron, variants-panel marker defect). R-008-safe (derived verdicts only). ✅ **Build-step-0 probe DONE 2026-06-06** ([findings](probe-findings-listing-aspects-2026-06-06.md)): Set/Number/Finish all EXIST (presence 75–100%/era) — design de-risked, work is value-normalization not redesign; corroborating Set/Number confirmed; bot line → near-zero (serve crawlers the baked TCGplayer AggregateOffer). Design amended with John's verdicts (k=4, slice 300/day, ~6h visitor-feed freshness, §4 quota portfolio, §9 two tranches). Quota: catalog-size-independent — only traffic moves the 5,000/day ceiling, and hitting it IS the eBay Growth-Check evidence (NOW #10). 7 ordered shippable build goals; #0 probe → #1 core → #2 per-card page close the bug end-to-end. ✅ **Tranche A #1 BUILT + calibrated 2026-06-07 (ships dark, no consumers wired):** `lib/listing/{normalize,identity,resolve}.ts` + `getListingDetail`; 3 test suites (871/0-fail); **calibration sweep** 207 cards → **66.7–68.1% coverage** (72.3% defensible), JP `117223259644` rejected (Language+Number), one normalizer false-reject fixed. Report: [calibration-resolver-2026-06.md](calibration-resolver-2026-06.md). ✅ **Decision-accuracy CERTIFIED 2026-06-07** (certification addendum, same doc): post-fix sweep **149 admits / 0 false-accepts · 56 identity-rejects / 0 false-rejects · 72.0% coverage**. The "16 corrupted `base2-*` entries" was a **MISREAD** — `base2` IS Jungle in pokemontcg.io (Base Set 2 is `base4`); the catalog is clean (now pinned by `lib/__tests__/catalog-qa.test.ts`). One real false-reject found + fixed (`detectGraded` now lets an explicit `Graded=No`/`Ungraded` veto a stray `Grade` aspect; validated zero-false-accept against 6 mis-tagged PSA slabs). Recoverable coverage → ~85-90% via **goal #2 SAFE levers** (finish-aware query recovered 12/12 vintage holos; shared-picker `"collection"`-collision fix; modest k bump) — **no identity-gate loosening**. **John reviews the certification before #2.** ✅ **Tranche A #2 + #3 BUILT 2026-06-11 (commits `1f0f3b2`/`33ad0c0`/`a0ed8c5`/`7c78211`) — NOT pushed/deployed; John reviews the I-009 measure + authorizes the deploy** (SESSION-LOG 2026-06-11): per-card page, `/go` redirect, and wishlist cron all on `resolveVerifiedListing`; finish-aware query lever live (vintage `Rare Holo` + explicit variants — coverage 69.1%→81.2% on the live sweep, neo1-17 JP item dead on both paths); variants-panel marker no longer cross-variant; honest-null states on every surface. **After deploy: every user-facing click is verified → the X bot becomes eligible to unpause** (per the 2026-06-06 tranche decision); Tranche B (#4–#7) gated on John's checkpoint. ✅ **#2/#3 DEPLOYED + live-verified 2026-06-11** (`63fe804`; neo1-17 JP item dead in prod, honest-null clean, content-marker live checks green). ✅ **`"collection"` prefilter collision fix BUILT + measured 2026-06-12 (pending John deploy)** — set-aware title-junk matching + audit-driven precision guards (title-graded drop, name corroboration; all prefilter-drop-only, identity gates + k=4 untouched): paired live sweep on the full 106-card `base6-*` slice went **17 → 104/106 verified · 0 lost · 0 known false-accepts** (the BEFORE arm's 17 included ≥2 false-accepts the fix also corrected); outlier filter not implicated post-fix. SESSION-LOG 2026-06-12. | Claude Code | ▶ Tranche A live · prefilter fix pending deploy |
| SUB | **Subscription-ready seam + instant-alerts wedge (gated)** <!-- added 2026-06-06, reconciliation per PLAN-2026-06-05; gated --> | Build the free/paid SEAM now so flipping subscription on later is a switch, not a rebuild ([ADR-059](DECISIONS.md#adr-059--utility-first-positioning--subscription-ready-not-paywalled), per PLAN-2026-06-05). **Free funnel is never paywalled.** | **Seam (build-now, low-risk):** an entitlement/tier check at the alert-frequency + watch-count boundaries in the watchlist/alerts code, defaulting everyone to **free**; keep watchlists email-anchored but structure the data so a user/account + tier flag layers on cleanly; instrument engaged-free-user count + which limits users hit (the measurable demand signal). **Wedge (flip-later):** "watch unlimited cards, get alerted the **instant** one drops below market" (near-real-time vs the free hourly/daily batch) + full deal list + grading-arbitrage lane + CSV export. ~$10/mo recurring (Stripe live-mode post-Mercury), framed "support the site + get deals first," **never** the founder-member tier. **Gate to build the seam:** now-ish (low-risk). **Gate to flip the wedge on:** an engaged-free-user threshold + observed demand (users hitting free limits or asking). Subsumes B.5. | Claude Code |
| 8 | **Per-card landing page generation pipeline** <!-- promoted via pivot 2026-05-23; Done 2026-05-30, Session 47.4 --> | Once #5 (one-card MVP) proves the design end-to-end. | ✅ **Done.** The 207 curated pages shipped (variants + sold-history chart + watchlist + Product schema + same-set internal links). Session 47.4 added the **tiered-rendering + ranked-expansion** pipeline ([ADR-046](DECISIONS.md#adr-046--tiered-per-card-rendering--catalog-expansion-to-1000-cards)) and ran the **first wave: 207 → 1,007 cards** (800 long-tail; ranked by SDK TCGplayer price across proven sets; 1006/1007 PokeTrace-matched). `scripts/{rank-candidate-cards,expand-catalog}.ts` regenerate on demand. | Claude Code | ✅ Done 2026-05-30 |
| 29 | **Catalog growth — Goal B then Goal C** <!-- added 2026-05-30, Session 47.4; rewritten Session 47.5 / ADR-047; modern-set wave 2026-06-25 / ADR-070 --> | **⚡ Update 2026-06-25 ([ADR-070](DECISIONS.md#adr-070--modern-set-catalog-expansion-unblocked-by-the-poketrace-only-movers-signal--the-volumematerality-filter)):** the **eBay-Browse-quota gate on expansion is MOOT for the insight product** — the market-movers signal (ADR-069) is PokeTrace-only, and per-card pages are demand-driven, so neither spends Browse quota. Shipped a **modern-set wave**: appended the 183 material (≥$5) chase cards from Prismatic Evolutions (`sv8pt5`), Surging Sparks (`sv8`), Mega Evolution (`me1`), Journey Together (`sv9`), Destined Rivals (`sv10`), Stellar Crown (`sv7`) via the rank/expand pipeline (extended with `--append`/`--min-score` so no live URL drops); catalog 1,007 → ~1,190. Chaos Rising (`me4`) deferred (in the SDK but unpriced). Expansion is now **unblocked + modern-prioritized** for the insight product; the remaining Browse-gated concern only applies to the single-listing board (curated tier). **Goal A** (architecture) PARTIALLY landed Session 47.5: ✅ metadata-only tier + ✅ resumable bake shipped; ❌ SSG+ISR + ❌ sitemap split **reverted** (the page's server-side `searchParams` read breaks ISR — `DYNAMIC_SERVER_USAGE`; Next has no sitemap index — see ADR-047 "Runtime reality"). | **Goal A.5 (new prerequisite):** move variant/condition selection **client-side** (`useSearchParams` in a client component; default state server-rendered) so the page is no longer forced dynamic by `searchParams`. This unblocks BOTH ISR and the sitemap re-split. **Goal B (Wave 2):** re-rank **all ~150 SDK sets** → ~5K priced longtail; gated on the resumable bake (now ~hours; `--resume` makes it safe). **Goal C (Wave 3):** ~18K via metadata-only + ISR; gated on Goal A.5 + Goal B + ISR cold-render telemetry ([R-013](RISKS.md#r-013--long-tail-per-card-render-cost-isr-blocked-by-searchparams)) + Browse-quota health ([R-012](RISKS.md)) + a correct sitemap re-split ([R-014](RISKS.md)). **Strategic note (2026-06-06, per BUSINESS-MODEL-2026-06-05):** catalog scale is the real **board-fattening lever** + the SEO long-tail surface — board richness comes from more cards, never looser filters. Catalog Wave 2 + the eBay Growth Check (NOW #10) go together: the deals cron is ~414/day with ~10× headroom before the 5,000/day ceiling, but Wave 2 + the structured-matching backbone (SM, above) will eat it, so the Growth Check gates further expansion. | Claude Code |
| 33 | **Content-pipeline reconciliation (engine write path = blog read path)** <!-- added 2026-05-31, Goal V.1 / R-015; Done 2026-05-31, Goal V.2 --> | HIGH + already-triggered ([R-015](RISKS.md#r-015--content-engine-write-path--blog-read-path-autonomous-posts-dont-go-live)): the engine wrote `app/blog/posts/`, the live blog read `app/(site)/blog/posts/`. | ✅ **Done (Goal V.2 / [ADR-049](DECISIONS.md#adr-049--content-pipeline-writeread-pinning--content-marker-verification-as-a-standing-closure-gate)):** single `lib/blog/posts-dir.ts` constant imported by all writers+readers; `posts-dir-consistency` test pins writer===reader; orphan `app/blog/posts/` deleted (byte-identical first) + `no-duplicate-blog-paths` guard; `hello-world.mdx` deleted; `content-marker-verification` closure gate added. | Claude Code | ✅ Done 2026-05-31 |
| 31 | **Goal A.5+A — ISR enablement (move variant/condition selection client-side)** <!-- added 2026-05-31, Session 47.5; John chose "defer" --> | Prerequisite that unblocks BOTH ISR rendering and the sitemap re-split (Goal C). Session 47.5 proved the server-side `searchParams` read in `app/(site)/cards/[slug]/page.tsx` forces dynamic rendering (`DYNAMIC_SERVER_USAGE` 500s under ISR). | Refactor the variant (`v`) + condition (`c`) picker to a Client Component that reads URL state via `useSearchParams`; render the default state server-side so the page no longer awaits `searchParams`. Must not regress the ADR-043 watchlist write path or the ADR-044 price-history chart. Then `revalidate` becomes legal for longtail/metadata-only (Goal A) AND a correct sitemap split (real index route + `/sitemap/*` in PUBLIC_ROUTES) is worth doing. See [ADR-047](DECISIONS.md#adr-047--ssgisr-hybrid-rendering--metadata-only-tier-for-the-18k-long-tail) "Runtime reality", [R-013](RISKS.md), [R-014](RISKS.md). | Claude Code |
| 34 | **C.2 — multi-creator automated ingestion + buy-signal input** <!-- added 2026-06-01, Goal C.1 --> | Gated on the C.1 pilot lift (validated: digest-injected generation cited 2 creators with full attribution, 0 violations; ADR-050). | Expand the whitelist beyond 5; harden the daily ingestion (cookies/proxy if CI is bot-blocked, [R-018](RISKS.md)); add sentiment-divergence (creator hype vs PokeTrace sold-trend) as a contrarian buy/sell input to the #32 buy-signal; wire `voiceCheck` (incl. em-dash detection) into the content-engine gate set (the C.1 pilot draft shipped 22 em dashes — voiceCheck isn't a hard gate yet); promote nickname-expansion candidates the digest surfaces. | Claude Code |
| 32 | **Goal B — buy-signal feature (condition-matched, re-mounted)** <!-- added 2026-05-31, Goal V; MVP 2026-06-01 / ADR-053; disabled then condition-matched + re-mounted 2026-06-01 via #32.1 --> | Surface a "is now a good time to buy?" signal on the per-card page, turning Foil from a price display into a buy recommendation (the deal-finder's core job-to-be-done). | ✅ **Done — shipped, disabled on a false-BELOW finding, then fixed + re-mounted via #32.1.** Full infra (`lib/buy-signal/{compute,reference,condition-infer,methodology-content}.ts`, badge, `/pricing-methodology`, Gate 13) live with condition-matched comparison. See #32.1 for the correctness fix. | Claude Code | ✅ Done 2026-06-01 |
| 32.1 | **Buy-signal correctness rewire — condition-matched ask + outlier guard** <!-- added 2026-06-01, post-verify of #32; Done 2026-06-01 / PATTERN I-009 --> | The #32 MVP compute was sound but the comparison basis was wrong (cheapest any-condition ask vs NM-weighted sold avg → false BELOW everywhere). Re-enable the badge only once it compares like-for-like. | ✅ **Done ([ADR-053](DECISIONS.md#adr-053--buy-signal-mvp--gate-13-anti-hype) closing amendment / PATTERN I-009).** New `lib/buy-signal/condition-infer.ts` infers the listing's condition from its title (graded / raw phrases / abbreviations; conservative → UNKNOWN on lots, foreign markets, proxies, or no keyword). `reference.ts` resolves the **same-condition** 30-day avg (never cross-condition); `compute.ts::classifyConditionMatched` adds the **outlier guard** (ask < 50% of the lowest raw-tier sold avg → UNKNOWN). New standing **live-smoke test** (`buy-signal-live-smoke.test.ts`) runs the full pipeline on real flagship listings and fails on any large-false-BELOW regression. Badge re-mounted on `/cards/[slug]` curated tier. Live-verified: the three flagships now read UNKNOWN (their listings carry no condition keyword → no badge, the honest outcome). | Claude Code | ✅ Done 2026-06-01 |
| 32.3 | **Buy-signal graded grade-specificity + grade-token disambiguation + symmetric outlier guard** <!-- added 2026-06-01 from the hit-rate scan; Done 2026-06-01, badge RE-ENABLED --> | The prod hit-rate scan (9.2% raw, ~4.8% defensible) caught ~9/19 rendering badges with I-009-signature deltas: coarse GRADED aggregate (PSA-9 vs PSA-10-inflated blend), "NM 7" → Near Mint abbreviation false-positives, and no graded outlier guard. Badge kill-switched OFF (`c2f3fe3`). | ✅ **Done — badge RE-ENABLED.** `condition-infer.ts` disambiguates grade tokens (`NM 7`/`MT 8`/`GEM MINT 10` → UNKNOWN; `PSA 9` → grade-specific `gradeKey`); `reference.ts` matches the SPECIFIC graded tier (`PSA_9` vs `PSA_9`, no blended fallback — verified PokeTrace exposes every service×grade); `compute.ts` gained a **symmetric outlier guard** ([0.5×, 2×] of the matched condition's sold avg → UNKNOWN outside the band); live-smoke expanded 3 → 8 incl. the rendering-badge corpus, asserts deltas in [−50, +100]. Two full-catalog re-scans: false BELOWs eliminated, then the symmetric guard cleared the 3 residual >±80% ABOVE cases (overpriced listings, not bugs). Final scan: **0 out-of-band badges**, deltas span −48.5%…+88.4%, all condition-matched. Re-enabled via the `BUY_SIGNAL_ENABLED` kill-switch (retained for rollback). | Claude Code | ✅ Done 2026-06-01 |
| B.6 | **Deal click-time redirect + self-hosted hero images** <!-- added 2026-06-05, B.4 follow-up; built, pending push --> | Conversion + polish before driving creator traffic. | ✅ **Built 2026-06-05 ([ADR-056](DECISIONS.md#adr-056--click-time-deal-redirect-godealslug--self-hosted-homepage-hero-images)) — pending John's push.** `/deals` "See it on eBay" now points at `/go/deal/[slug]`, which runs a LIVE `getBestListing` at CLICK time and 302s to the specific best listing (search fallback) — one Browse call per click, nothing persisted (R-008), `deals_redirect` surface, `dl-` customid. Slug validated vs catalog; destination always an internally-built eBay URL (no open redirect). Homepage hero images self-hosted to `public/hero/*.webp` (the flaky `images.pokemontcg.io` CDN was the "broken row" — premise's "404 bottom row" didn't reproduce; all 8 were 200 hero cards). Gates: tests (redirect resolve/fallback/slug-validation/customid + no-broken-image structural) 781/0-fail, compliance 6/6, tsc + build clean, design:lint 0-new, /security-review. ✅ **DEPLOYED + live-verified 2026-06-05 (commit `4ce1cd8`).** On foiltcg.com: all 8 board CTAs wire to `/go/deal/`, each BELOW deal 302s to a specific eBay item with `customid=dl-<slug>`, a bogus/`evil.com` slug both 302 to internal `/deals` (no open redirect), and the homepage serves 8 self-hosted hero webp (zero `images.pokemontcg.io`). | Claude Code | ✅ Done 2026-06-05 |
| 32.2 | **B.2 — roll the buy-signal badge to the listing + search surfaces** <!-- added 2026-06-01; P0 premise check HELD pending #10 --> | **GATED — held by a P0 premise check 2026-06-01.** The badge needs a live eBay ask + the listing title for condition inference, so it works on the `force-dynamic` per-card page but NOT on the `force-static` catalog grids (`/cards`, `/cards/sets/[set-id]`) or the client-side search — putting it there would fire one Browse call **per card tile** at render (16–100+ per set page), forcing those pages dynamic (R-008 forbids caching the eBay side), blowing eBay's ~5,000/day ceiling, and endangering the pending Growth Check. See [R-012](RISKS.md#r-012--ebay-browse-quota-concentration-at-1k-routes). | **Gate: ROADMAP #10 (eBay Growth-Check ceiling lift) MUST land first, AND ≥1 week of prod hit-rate data from `/cards/[slug]`** (what fraction of curated cards actually produce a non-UNKNOWN badge — if the per-card badge rarely fires because most listings lack a title-inferable condition, a grid rollout is low-value regardless of quota). Only then choose the rollout shape (live-ask per-tile vs a cacheable market-read variant). Held per the I-009 lesson: don't ship a signal whose semantics or cost don't hold up. | Claude Code |
| B.4 | **Buy-signal leaderboard page (`/deals`)** <!-- added 2026-06-02; BUILT 2026-06-05, pending deploy+John --> | Architecture revision unblocks the rollout that the 2026-06-01 P0 premise check held against R-012. | ✅ **Built 2026-06-05 ([ADR-054](DECISIONS.md#adr-054--todays-best-deals-leaderboard-precompute-and-cache-derived-metadata-never-persist-ebay-listing-data)) — pending deploy + John's push.** Public `/deals` "Today's best deals" board, free/affiliate/no-paywall, the X-bot screenshot surface. **R-008 resolved by persisting the SIGNAL not the listing:** daily cron `/api/cron/deals-refresh` classifies the live ask then discards it; the `buy_signals` cache stores only derived (`signal`,`delta_pct`) + PokeTrace (`sold_reference`,…) + SDK-catalog (`card_name`/`set_name`/`image_url`) fields — **no eBay listing column** (EBAY-COMPLIANCE row #13). Board reads cache only (zero view-time Browse calls, R-012); live listing resolves on an affiliate CTA click. Ranked by below-market %, BELOW-only, confident (n≥5). Shared `computeCardBuySignal` so the per-card badge + board can't drift (I-008). Linked from header nav + homepage primary CTA. Gates: premise ✅, tests (deals-leaderboard + deals-refresh-batch + proxy + telemetry) 792/0-fail, compliance 6/6, tsc clean, build clean, design:lint 0-new. ✅ **DEPLOYED + live-verified 2026-06-05 (commit `0381900`).** `buy_signals` migration applied to prod (RLS on, service-role-only); `/api/cron/deals-refresh` populated 207 rows; `https://foiltcg.com/deals` renders the ranked condition-matched deals (no eBay call at view time). The board honestly shows ~3 deals after F4's condition+language gating — small because the catalog is small (~2% genuine-deal rate), not because anything is broken; it scales with catalog size. | Claude Code | ✅ Done 2026-06-05 |
| X1 | **Daily X content bot (dry-run-first)** <!-- added 2026-06-05, ADR-058; built dry-run, pending push + John enable --> | Daily X traffic before/around the PokeBeard pilot; the /deals board is the screenshot surface. | ✅ **Built dry-run-default 2026-06-05 ([ADR-058](DECISIONS.md#adr-058--daily-x-content-bot-dry-run-first-own-posts-only-satori-image-not-playwright)) — pending push; live posting needs John.** Daily cron `/api/cron/x-post` (14:00 UTC): rotates 3 angles (deal-of-day / price-spotlight / educational), renders a branded 1080×1350 portrait from the `buy_signals` cache via **Satori/next-og** (not Playwright — won't run in a Vercel cron; R-008-safe), generates a **Gate-12/13-voice-gated** post, then **dry-run** posts the draft to Discord `#content-engine` (text + image) — **`X_BOT_LIVE=false` default; no code path posts to X until John flips it.** Single X API boundary (`lib/social/x-client.ts`, OAuth 1.0a, soft-fail). Gates: premise ✅, tests (rotation + dry-run-never-posts + live-posts + voice gate + boundary) 823/0-fail, compliance 6/6, tsc + build clean, design:lint 0-new. ✅ **DEPLOYED in dry-run + verified 2026-06-05/06 (commits `93d63fc`, `6c5eecd`).** `/api/cron/x-post` live + bearer-gated (401 without); triggered with the bearer → `{ok:true, live:false, posted:false, reason:"dry_run"}` — **posts NOTHING to X** (safety invariant holds in prod); the generated deal-of-day post was voice-clean. **Remaining (John, manual — see the **X-bot go-live** row in NOW):** create the X app + OAuth1 user tokens + a console spending cap, smoke-test once, set `X_BOT_LIVE=true` (runbook: docs/runbooks/x-bot.md). New risk [R-019](RISKS.md#r-019--x-automation-tos--api-cost-runaway). | Claude Code + John | ✅ Built + dry-run-live 2026-06-05 |
| F4 | **Buy-signal like-for-like via eBay item specifics (condition + language/market)** <!-- added 2026-06-05, ADR-057; built, pending push --> | Correctness fix (false cross-market deals) + coverage lift, before the X bot posts live. | ✅ **Built 2026-06-05 ([ADR-057](DECISIONS.md#adr-057--buy-signal-like-for-like-via-ebay-item-specifics-condition-coverage--languagemarket-gate)) — pending John's push.** P0 live probe confirmed `getItem.localizedAspects` exposes Card Condition + Language + Graded/Grade (search has none). New `getListingAspects` (R-008: compute-time read, no persist) + pure `aspects.ts` reader; `condition-infer` now prefers the eBay Card Condition/Grade aspect and **gates on Language=English** (the Japanese-Alakazam cross-market false-positive). Wired into the per-card page + deals cron. **MEASURE (live, 207 cards): board 5→3 BELOW — 2 false positives removed** (a Flareon multi-card lot; an unconfirmable-language Charizard V), 0 gained this run; stated honestly. Graded slabs now classify via the Grade aspect (coverage). Fixed a latent gate gap (deals/aspect tests weren't in `npm test`) → suite 781→**812**. Gates: probe ✅, unit tests on the aspect reader (incl. real Japanese Alakazam shape) + live-smoke 8/8, compliance 6/6, tsc + build clean, design:lint 0-new, /security-review. ✅ **DEPLOYED + live-verified 2026-06-05 (commit `3c45aeb`).** Prod deals cron re-classified aspect-gated: `belowCount 4→3`; the Japanese **Alakazam ex (sv3pt5-201) flipped BELOW→UNKNOWN** (Language gate excludes the Japanese SV2a listing) — the live `/deals` board shows 0 Alakazam, just 3 clean English NM deals. Cross-market false positive gone in prod. **v2 (deferred):** same-language reference so non-English listings classify against their own market instead of excluding. | Claude Code | ✅ Done 2026-06-05 |
| B.5 | **One-time-unlock monetization (Collectrics-style)** <!-- added 2026-06-02, monetization, gated; SUBSUMED 2026-06-06 per PLAN-2026-06-05 --> | GATED on 4-8 weeks of real traffic + post-PokeBeard-pilot engagement signal confirming feature demand. | ⚠️ **SUBSUMED / clarified 2026-06-06 ([ADR-059](DECISIONS.md#adr-059--utility-first-positioning--subscription-ready-not-paywalled), per PLAN-2026-06-05):** the **recurring instant-alerts subscription** (the "subscription-ready seam + instant-alerts wedge" row) is the **primary** paid path; a one-time unlock is **optional, not the plan**. Do not revive the founder-member tier. Affiliate (EPN) stays primary for free/organic traffic; the free funnel is never paywalled. Original spec retained below for the optional case. One-time-unlock monetization (Collectrics-style). Single $15-20 unlock for a Pro feature bundle (e.g. leaderboard access, instant alerts, CSV export of watchlist history, advanced filters). Framed as 'support the site' not 'pay for premium' per Collectrics' observed model. Stripe checkout, one-time payment, no recurring billing. GATED on: 4-8 weeks of real traffic data + post-PokeBeard-pilot engagement signal confirms feature demand. Do not bring back the founder member tier. Affiliate revenue (EPN) remains the default/primary monetization for organic traffic. | Claude Code |
| 9 | **Wishlist alert cron** <!-- Done 2026-05-24, Session 27 --> | Closed via [Session 27](SESSION-LOG.md) — hourly Vercel Cron Job at `/api/cron/wishlist-alerts` walks watchlists, dedups Browse calls per slug, sends Resend emails when current price ≤ target, stamps `last_notified_at`. See [ADR-024](DECISIONS.md#adr-024--wishlist-alert-cron-on-vercel-cron-jobs-vs-github-actions-or-supabase-edge-functions). | Claude Code | ✅ Done 2026-05-24 |
| 10 | **Content engine reframe → "Best [card] deals this week"** <!-- promoted via pivot 2026-05-23 --> | Once #5 + #6 land — the content posts will link into the per-card pages. | Update `SYSTEM_PROMPT` in `lib/seo/content-engine.ts` from market-analysis framing to buyer-intent framing. Topic backlog in `docs/seo-strategy.md` reshuffles toward "[card] price under $X" / "Best [set] deals" topics. Mon/Thu cron + gates pipeline stays exactly as-is per ADR-020. |
| 11 | **9th quality gate: "Citable claim density"** — 8+ standalone factual statements per post | AI Overview optimization. Google's SGE pulls atomic sentences that read as standalone facts; current gates reward presence of $/dates/Foil-cites but not the citable-sentence shape. | `lib/seo/quality-gates.ts` + positive/negative test in `lib/__tests__/seo-quality-gates.test.ts`. Heuristic: count sentences ≤ 25 words that contain a named entity + a verb + a specific noun. |
| 12 | **Content engine prompt: AI Overview citation discipline** | Same trigger as gate 11. Prompt needs to bias toward short declarative sentences with named entities. | Edit `SYSTEM_PROMPT` in `lib/seo/content-engine.ts`. Add a "Citable claim" rule. Lands alongside #10's reframe. |
| 13 | **Run `searchfit-seo:ai-visibility` baseline** | Once domain is GSC-verified. Establishes the "before" snapshot we'll re-measure monthly. | Document the report location in `docs/SESSION-LOG.md`. |
| 14 | **Expand `seo-strategy.md` cluster topics for deal-finder framing** | Backlog currently has ~35 cluster topics from the valuation framing. Need to recompose around buyer-intent queries — "[card] for sale," "cheap [card] for sale," "[set] booster value," etc. | Hand-curate from competitive-gap reports + the [card] for sale long-tail. |
| 15 | **Scrydex API migration evaluation** <!-- reframed via pivot 2026-05-23 --> | Triggered by Pokemon TCG SDK gaps in #8 OR PokeTrace rate limits. Scrydex has per-card endpoints we'd use for richer per-card landing pages (price history, sold-comps). | Tracked in [DECISIONS.md](DECISIONS.md). Now downstream of the deal-finder direction rather than scanner-driven. |
| 16 | **Slack (or Discord) ops workspace expansion** | [ADR-014](DECISIONS.md#adr-014--outbound-discord-notifications-per-channel-webhooks-soft-fail-single-import-boundary) wired the four Foil HQ channels. Next: wire Stripe events, ebay-affiliate clicks (sampled), wishlist alert send-volume per cron. | Mostly already plumbed via `lib/notifications/discord.ts`; this is "wire one more producer per concern." |
| 30 | **Newsletter-draft review for the two Session-47.4 smoke-test posts** <!-- added 2026-05-30, Session 47.4 --> | The deploy-fix smoke tests shipped two real autonomous posts (`1dc2cca` + `c91b794`, both live). The Mon/Thu pipeline normally emails John a paste-ready newsletter draft per post (ADR-012); confirm those arrived and review/publish or discard as usual. Low effort — the drafts auto-generate; this is just the human review step. | John (review `docs/newsletter-drafts/` + inbox) |
| 28 | **Task #28 — Home page redo with design skills** <!-- added 2026-05-28, Session 44 followup; Done 2026-05-28, Sessions 45+46 --> | Closed across two sessions. Session 45: `/impeccable teach`+`document` (PRODUCT.md + DESIGN.md), then critique→audit→animate→distill→clarify→polish for a11y + hero focus (30→34/40). Session 46: `/impeccable bolder`+`polish` warmth pass — Fraunces display, holofoil spark mark, Founding-Member pricing removed, lighter asymmetric hero showcase, light decorative card peeks ([ADR-036](DECISIONS.md#adr-036--home-page-warmth-pass-fraunces-display-spark-mark-pricing-removal-lighter-scrim)). Drift guards extended in `lib/__tests__/visual-regression.test.ts`. Home page is launch-complete. | Claude Code | ✅ Done 2026-05-28 |
| 27 | **Task #27 — Per-card page variant selector** <!-- added 2026-05-27, Session 43 followup; queued behind #28 after Session 44 --> | Session 43 ([ADR-033](DECISIONS.md#adr-033--homepage-hero-card-backdrop-treatment-grail-row-behind-frosted-cream)) seeded the homepage hero with 8 modern grail cards across 7 sets — each one has 2-4 priced variants in `tcgplayerPrices` (Normal / Holofoil / Reverse Holo / 1st Edition / Rainbow / etc). The Session 41 `<CardVariantsSection>` already RENDERS those variants as a stacked PriceRangeBar list, but there's no INTERACTIVE selector — you can't switch the best-listing block + watchlist form to pin the targeted variant. Task #27 ships a `<VariantSelector>` Client Component on `/cards/[slug]` that updates URL state (`?variant=holofoil`) and re-keys the best-listing block + watchlist target-price input to the selected variant. Scope: ALL new code in `/cards/[slug]` — no schema change (URL state only), no DB change, no server-action change. **Queued behind Task #28 (home page redo) as of Session 44 — the home page is the higher-leverage Twitter-launch surface.** Trigger to start: after Task #28 lands. | Claude Code |

---

## LATER — 1-3 months (2026-06-11 → 2026-08-20)

| # | Item | Why later |
|---|------|-----------|
| 17 | **V2 — Scanner relaunch as a deal-finder companion surface** <!-- moved via pivot 2026-05-23 --> | Per [ADR-020](DECISIONS.md#adr-020--pivot-to-buyer-side-deal-finder-positioning), the scanner code stays in-tree (`app/upload/`, `lib/vision*.ts`, `lib/poketrace.ts`, detect→identify→confirm pipeline). V2 surface is "snap a card → land directly on its `/cards/[slug]` deal page" — completes the loop between the scanner and the deal-finder product. Triggers V2 once V1 deal-finder is producing affiliate revenue. |
| 18 | **Content syndication: Reddit r/PokemonTCG, Medium, Substack republish** | Owned channels first; syndication adds reach but only matters once we have ≥10 posts worth republishing. |
| 19 | **A/B test Gemini 3.1 Pro on the visual-confirm pass** | Sonnet 4.6 confirms work well; Gemini 3.1 Pro is ~8× cheaper at similar quality on side-by-side image tasks. Worth measuring once we have ≥1K confirm-pass calls of baseline data. Re-prioritizes once #17 (scanner V2) approaches launch. |
| 20 | **HowTo + Product + Review schema rollout** | Pillars and posts currently emit Article + FAQPage only. Adding HowTo (for guides), Product (extra on per-card landing pages — beyond the basic Product markup in #5), Review (for graded comps) widens SERP feature eligibility. |
| 21 | **Monthly AI Visibility tracking cadence** | Manual snapshot every month-end via searchfit-seo. Compare deltas. |
| 27 | **`scan_cards` per-card persistence table** <!-- scanner stack, deferred to V2; renumbered from #22 in Session 39 to free that slot for the visual identity overhaul -->  | Currently `scans` only stores image metadata; per-card identification results live in transient `scanResults`. Persisting unlocks accuracy diagnostics, "your scan history," and the `mostScannedCards` data-injection helper. Schema TBD. Lands with #17 scanner relaunch. |
| 28 | **Manual spot-check OR 24-hr noindex window before autonomous posts are search-visible** <!-- Renumbered from #23 to #28 in Session 40 to free #23 for the pre-launch fix pass --> | Risk mitigation for [content engine fabrication](RISKS.md#r1). Trigger: first time the gates pass something embarrassing OR sustained organic traffic begins. |
| 24 | **Vercel Pro Trial decision** | Trial expires 14 days from activation date. If we're on it, log expiry to ROADMAP NOW the day it falls within 7d. |
| 26 | **TCGplayer affiliate plumbing** <!-- promoted via pivot 2026-05-23 --> | V1.5 — eBay-only is V1, TCGplayer is the planned second affiliate source per ADR-020. Plumbed through `lib/affiliate/links.ts` (designed in #6 to accommodate). Triggers once TCGplayer affiliate approval lands. |

---

## PARKED — explicitly deferred

| Item | Reason parked | Re-check trigger |
|------|---------------|------------------|
| Google OAuth login | Needs domain whitelist + LLC formation | Post-LLC banking |
| Stripe live mode | Needs LLC banking | Post-LLC banking |
| Multi-TCG (MTG, Yu-Gi-Oh) | Distracts from Pokémon wedge | $5K MRR |
| WebSocket price streams | PokeTrace Scale tier feature | Pro waitlist > 100 |
| Sold-listings endpoint | PokeTrace Scale tier feature | Pro waitlist > 100 |
| Programmatic per-card landing pages (large catalog beyond top-200) | Promoted to NOW (#5) for top-1 / NEXT (#8) for top-200 via [pivot 2026-05-23](DECISIONS.md#adr-020--pivot-to-buyer-side-deal-finder-positioning). Large-catalog (1K+ cards) remains parked behind Scrydex migration. | Scrydex migration (item #15) |
| Anomaly detection beyond PokeTrace flags | Insufficient scan volume to ground heuristics | 10K scans |

---

## How to use this doc

- **Adding an item:** which bucket fits? NOW = blocking this week's ship. NEXT = queued behind NOW. LATER = direction but no week assigned. PARKED = "no, not now."
- **Removing an item:** move to a SESSION-LOG entry naming the commit that closed it. Don't silently delete — the history matters for understanding past decisions.
- **Re-prioritizing:** edit the table in place. If something moves from NEXT to NOW because external state changed, drop a `<!-- promoted from NEXT 2026-05-22 -->` comment on the row.
